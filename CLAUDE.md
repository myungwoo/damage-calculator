# Mapleland Damage Calculator

메이플랜드 데미지 계산기 웹 애플리케이션

## 기술 스택

- **프론트엔드**: Next.js 15, React 19, TypeScript, Tailwind CSS
- **UI 라이브러리**: @headlessui/react, lucide-react
- **개발 도구**: ESLint, Prettier

## 프로젝트 구조

```
app/
  components/          # React 컴포넌트
    DamageCalculator.tsx   # 레이아웃 오케스트레이터 (헤더 + 입력 2열 + 결과 레일)
    MonsterDropdown.tsx    # 몬스터 선택 드롭다운 (검색어 유지, 지역 그룹, 키보드 조작)
    MonsterSprite.tsx      # 몬스터 그림 (maplestory.io 렌더 API, 접었다 편다)
    NumberInput.tsx        # 숫자 입력 컴포넌트
    SaveSlots.tsx          # 헤더의 저장 슬롯 팝오버
    ThemeToggle.tsx        # 다크/라이트 모드 토글
    panels/                # 입력 패널
      MonsterPanel.tsx
      CharacterPanel.tsx
      EquipmentPanel.tsx
      SkillsPanel.tsx
    results/               # 결과 패널
      ResultsPanel.tsx
      KillProbabilityChart.tsx
      KnockbackProbability.tsx    # 넉백 확률 (라인 하나 기준)
      AvoidProbabilities.tsx      # 물리 / 마법 회피 확률
      HitDamage.tsx               # 물리 / 마법 피격 데미지
      MobSkillNotice.tsx          # 방컷을 나쁘게 만드는 몹 스킬 경고
      HitProbabilityNotice.tsx    # 타격 확률 (100% 미만일 때만)
    ui/                    # 재사용 프리미티브
      Card.tsx
      Field.tsx
      ReadonlyValue.tsx
      SegmentedControl.tsx
      SkillRow.tsx
      Toggle.tsx
  data/                # 정적 데이터
    monsterPresets.ts      # 몬스터 프리셋 데이터 (id = 원작 Mob.wz 몹 ID)
    skillEffects.ts        # 스킬 효과 데이터
    mobBuffs.ts            # 몹 공격업 단계별 배율
    mobSkills.ts           # 몹 스킬 ID -> 이름 · 영향 · 설명
    shadowShifter.ts       # 페이크(쉐도우 쉬프터) 레벨별 확률
    standardPDD.ts         # 도적 레벨별 표준 물리 방어력 (피격 데미지용)
    venom.ts               # 베놈 레벨별 수치와 상수
    weapons.ts             # 무기 데이터
  hooks/               # React 훅
    useCalculatorState.ts  # 계산기 상태 관리
    useMonsterHistory.ts   # 소환 링크 이동을 뒤로가기로 되돌리기
  types/               # TypeScript 타입 정의
    calculator.ts
  utils/               # 유틸리티 함수
    calculatorUtils.ts     # 계산기 헬퍼 함수
    damageCalculator.ts    # 데미지 계산 로직
    fft.ts                 # FFT 알고리즘
    venom.ts               # 베놈 누적 도트 데미지 분포 (마르코프 DP)
  constants/           # 상수 정의
    calculator.ts
  page.tsx             # 메인 페이지
  layout.tsx           # 레이아웃
  globals.css          # 전역 스타일
tests/                 # 유닛 테스트 (node --test)
  helpers/
    reference.ts           # 방컷 확률 참조 구현 (O(n^2) DP, 몬테카를로)
  avoidProbability.test.ts
  damageCalculator.test.ts
  knockbackProbability.test.ts
  fft.test.ts
  hitDamage.test.ts
  venom.test.ts
public/                # 정적 파일
```

## UI 구조

핵심 원칙은 **입력을 만지는 동안 결과가 항상 보여야 한다**는 것이다.
데미지와 방컷 확률은 값을 하나 바꿀 때마다 확인하는 값이라, 스크롤해서 찾아가야 하면
계산기로서 제 역할을 못 한다.

- 데스크톱(`lg` 이상): 왼쪽에 입력 2열(몬스터·캐릭터 / 장비·스킬), 오른쪽에
  `sticky` 결과 레일. 방수가 많아 결과가 화면보다 길어지면 레일 안에서 스크롤한다.
- 그 아래 너비: 입력이 한 줄로 쌓이고 결과는 하단 고정 바로 접힌다.
  바에는 방컷 요약과 총 데미지 범위만 띄우고, 누르면 시트로 펼쳐진다.

### 디자인 토큰

색은 전부 `globals.css`의 CSS 변수로 두고 `tailwind.config.ts`에서
`rgb(var(--x) / <alpha-value>)`로 받는다. 변수 값은 `"R G B"` 형식이어야
`bg-brand/10` 같은 투명도 유틸리티가 동작한다.

| 토큰 | 쓰임 |
| --- | --- |
| `bg` / `card` / `sunken` / `line` | 배경 레이어와 경계선 |
| `field` / `field-line` / `field-disabled` | 입력칸 표면 / 테두리 / 비활성 표면 |
| `ink` / `muted` | 본문 / 보조 텍스트 |
| `brand` | 브랜드 색. 기본 강조, 방컷 확률 막대 |
| `crit` | 크리티컬, 경고 |
| `venom` | 베놈 관련 값 |
| `danger` | 삭제, 타격 확률 미달 |

**`field`는 `sunken`과 반드시 구분해서 쓴다.** `sunken`은 패널 배경(스킬 행, 요약 박스,
막대 트랙)이고 `field`는 값을 직접 고칠 수 있는 표면이다. 예전에는 입력칸이 `sunken`을
같이 써서 라이트 모드에서 회색으로 채워졌는데, 카드가 흰색이라 **입력칸이 비활성으로
보였다**. 게다가 비활성 상태(`bg-line/25` ≈ `#ECEFF3`)가 활성 상태(`#F0F3F7`)와
채널당 4밖에 차이가 안 나서 둘이 구분되지도 않았다.

지금은 이렇게 나눈다.

| 상태 | 라이트 | 다크 | 읽히는 방식 |
| --- | --- | --- | --- |
| 입력 가능 | 흰색(카드와 동일) + 또렷한 테두리 | 카드보다 밝은 면 + 또렷한 테두리 | 테두리가 경계를 만든다 |
| 비활성 | 회색으로 채움 + `line` 테두리 | 카드 쪽으로 눌러 채움 + `line` 테두리 | 대비가 정확히 뒤집힌다 |
| 계산값 (`ReadonlyValue`) | `sunken` + 점선 테두리 | 〃 | 입력칸의 반대 조합 |

`field-line`은 각 테마의 입력칸 배경 대비 약 3:1이라 WCAG 1.4.11(비텍스트 대비)을
만족한다. 기존 `line`은 라이트 1.2:1 / 다크 1.4:1이라 양쪽 모두 테두리가 사실상 안 보였다.
버튼(`.ghost-button`)은 반대로 `sunken`으로 채우고 테두리만 `field-line`을 쓴다 —
**채워짐 = 누르는 것, 비어 있음 = 쓰는 것**이 되도록 한 것이라 뒤집지 말 것.

토글 꺼짐 상태의 손잡이는 `card`가 아니라 `muted`다. `card`로 두면 라이트에서는 흰
손잡이가 밝은 트랙에, 다크에서는 어두운 손잡이가 어두운 트랙에 묻혀 양쪽 다 안 보인다.

다크 모드는 `:root.dark`에서 같은 변수를 덮어쓴다. 첫 페인트에서 색이 튀지 않도록
`layout.tsx`의 인라인 스크립트가 하이드레이션 전에 클래스를 붙이고,
`ThemeToggle`은 그 값을 읽어서 시작한다.

반복되는 조합은 `@layer components`의 `.card` / `.field-input` / `.field-trigger` /
`.field-readonly` / `.field-label` / `.chip` / `.ghost-button`에 모아 뒀다.
새 입력칸은 이 클래스를 쓴다. 눌러서 여는 입력칸형 컨트롤(몬스터 드롭다운)은
`.field-trigger`로 같은 표면을 받는다.

### 컴포넌트 규칙

- **스킬 레벨은 `SkillRow`로 통일한다.** 슬라이더(훑기) + 숫자 입력(정확히 찍기) +
  M 버튼(만렙)을 같이 준다. 레벨 범위는 `getSkillLevelRange`를 단일 출처로 삼아
  뽑으므로, 스킬 레벨 상한이 바뀌면 그 함수만 고치면 된다.
  M 버튼은 항상 그 범위의 최댓값으로 간다(메이플 용사는 30).
- **선택지가 넷 이하이고 성격이 다른 값은 `SegmentedControl`을 쓴다.**
  공격 스킬이 여기 해당한다. 후보를 전부 보여줘서 한 번의 클릭으로 바뀐다.
  자주 만지지 않는 곁다리 옵션(방어업 단계)은 `dense`로 한 줄만 쓴다 —
  부가 정보가 라벨 아래가 아니라 오른쪽에 붙어서 높이가 절반이 된다.
- **계산으로 정해지는 값은 `ReadonlyValue`로 그린다.** 점선 테두리로 입력칸과
  구분한다. 총 스탯, 공격력 합, LUK 순 스탯이 여기 해당한다.
- **공격력 효율은 공격력 합 상자 안에 구분선으로 갈라 둔다.** 공격력 1당 LUK과
  LUK 1당 공격력(둘 다 소수 다섯 자리)은 공격력 합과 LUK 합계의 나눗셈이라 그 자리에서
  바로 나오는 값이다. 카드를 따로 세우면 제 중요도보다 커 보이고, 멀리 떼면 어느 합에서
  나온 값인지가 안 보인다. **더 자주 찾는 "공격력 1당 LUK"을 먼저 세운다.**
  어느 한쪽 합이 0이면 나눌 수 없으므로 줄째로 뺀다 — 0으로 적으면 "효율이 0"으로 읽힌다.
- **방컷 확률 막대의 숫자는 막대 밖 고정 열에 둔다.** 막대 위에 얹으면 채워진 구간과
  빈 구간에서 대비가 달라져 글자가 읽히지 않는다.
- **결과 레일의 순서는 "방컷 요약 -> 방컷 확률 막대 -> 구분자 -> 데미지 값들 -> 회피 확률 -> 피격 데미지"이다.**
  방컷 확률은 이 계산기가 존재하는 이유라 요약 바로 아래에 온다. 데미지 값들 뒤로 밀면
  제일 중요한 정보를 스크롤해서 찾아가야 한다. 데미지 값들 앞에는 구분자(`border-t`)를 둬서
  "확률"과 "데미지"의 성격 차이를 눈으로 갈라 준다.
- **타격 확률과 회피 확률은 입력 패널이 아니라 결과 레일에 둔다.** 둘 다 **입력이 두
  패널에 걸쳐 있기 때문이다** — 타격 확률은 캐릭터 명중률과 몹 회피율에서, 회피 확률은
  캐릭터 회피율 · 페이크와 몹 명중률에서 나온다. 어느 한쪽 입력 패널에 두면 반드시
  반대쪽 입력을 만지는 동안 화면 밖으로 나간다(예전에 몬스터 패널에 뒀을 때, 캐릭터
  패널의 페이크 토글이 자기 효과를 못 보는 상태였다). sticky 레일에 있으면 양쪽 어디를
  만져도 계속 보인다. 모바일 하단 요약 바에는 넣지 않는다(방컷 요약 + 총 데미지 범위 유지).
- **타격 확률은 100% 미만일 때만 띄운다.** 명중률이 충분한 구간에서는 늘 100%라
  자리만 차지하고, 저레벨이나 고레벨 몹을 칠 때만 잠깐 중요해진다. 뜰 때는 방컷 확률 막대
  **바로 아래**에 경고색(crit / danger)으로 붙인다. 막대가 이미 이 확률을 반영해 내려간
  값이고, 100% 미만 구간에서는 명중률 근사식 때문에 계산 오차까지 커지므로
  경고 대상 옆에 있어야 읽힌다.
- **피격 데미지는 회피 확률 바로 아래에 붙인다.** 둘은 "몹이 때리면 어떻게 되나"라는
  한 질문의 앞뒤다 — 회피 확률만 보면 맞았을 때 얼마나 아픈지를 모르고, 피격 데미지만
  보면 그게 얼마나 자주 들어오는지를 모른다. 입력이 캐릭터(방어력·스탯)와 몬스터(공격력)
  양쪽에 걸쳐 있어 sticky 레일에 있어야 하는 이유도 회피 확률과 똑같다.
  모바일 하단 요약 바에는 넣지 않는다.
- **넉백 확률은 총 데미지 범위 카드 안에 한 줄로 얹는다.** 방컷 확률이나 데미지처럼
  매번 확인하는 값이 아니라 곁다리 정보라, 카드를 따로 세우면 제 중요도보다 커 보인다
  (막대까지 붙였더니 실제로 그랬다). 그렇다고 멀리 떼어 놓으면 안 되는데, 같은 데미지에서
  나오면서 **눈금이 다르기 때문이다** — 총 데미지 범위는 시전 1회 **합계**이고 넉백은
  **라인 하나**다. 떨어뜨려 두면 "총 데미지가 넉백 수치보다 큰데 왜 100%가 아니냐"가 된다.
  그래서 같은 카드 안에 구분선으로 갈라 두고, 라벨 옆에 판정 기준(`타격 하나 N 이상`)을
  같이 적는다. 넉백 수치를 모르는 직접 입력 몬스터에서는 줄째로 뺀다.
- **몬스터 드롭다운의 검색어는 닫아도 지우지 않는다.** "자쿰"으로 좁혀 놓고 자쿰 ·
  자쿰팔을 번갈아 골라 보는 게 흔한 사용 방식이라, 닫을 때마다 지우면 매번 다시
  타이핑해야 한다. 대신 다시 열 때 입력칸 글자를 통째로 선택해 둬서 새로 검색할
  사람은 타이핑 한 번으로 덮어쓰고, 지우기(X) 버튼으로 전체 목록으로 바로 돌아간다.
- **없는 값을 화면에 만들지 않는다.** 마법 공격이 없는 몹(97종)은 마법 공격력이 0보다
  커도 마법 피격 데미지와 마법 회피 확률을 **아예 그리지 않는다.** 실제로 들어올 수 없는
  숫자를 띄우면 유저가 그 값을 기준으로 판단하게 된다.
  프리셋에 `hasMagicAttack`이 없으면 "모른다"가 아니라 **"없다"** 는 뜻이다 — 처음에
  이 둘을 반대로 읽어서 파풀라투스에 없는 마법 데미지가 떴다. 값을 모르는 것은 직접
  입력 몬스터뿐이라, 그때만 `true`로 두고 보여준다.
- **방어업 입력은 몬스터 패널에, 잠긴 프리셋 칸과 선 하나로 갈라서 둔다.** 몹 수치가
  아니라 "이 상황을 가정하고 계산해 달라"는 입력이라 프리셋 칸에 섞으면 몹 데이터로
  읽힌다. 그 몹이 실제로 방어업을 쓰는지는 **라벨 옆 빈 자리**에 적어서 줄을 늘리지
  않는다. 자주 만지는 값이 아니므로 `SegmentedControl`의 `dense`(한 줄, 수치는 라벨
  오른쪽)를 쓰고, 걸리는 시점은 **라벨 + 숫자칸 한 줄**로 둔다. 같은 정보를 담고도
  세로 공간이 절반이 된다.
- **입력칸을 문장 한가운데 끼워 넣지 않는다.** "[3]방부터 데미지가 준다"처럼 쓰면
  글줄과 네모칸의 높이가 안 맞아 눈에 걸린다. 단위는 입력칸 안 `suffix`로 넣고
  (`분당 공격 횟수`의 `회/분`과 같은 방식) 라벨은 왼쪽에 세운다.
- **소환 대상 이름은 눌러서 그 몹으로 넘어가고, 뒤로가기로 돌아온다.** 주소는 건드리지
  않는다 — `pushState`에 URL을 안 넘기면 지금 주소 그대로 항목만 쌓인다. 자세한 내용과
  주의점은 아래 "몬스터 이동 히스토리" 절에 있다.
- **몹 스킬은 칩 색으로 성격을 가른다.** 방컷을 나쁘게 만드는 것(`kill`)만 빨간색이고,
  그 몹에는 방컷 확률 아래에 경고 한 줄이 더 붙는다. 칩만 훑어도 "이 몹은 계산대로 안
  잡힐 수 있다"가 읽혀야 한다.
- **몬스터 그림은 기본으로 접어 둔다.** 항상 띄우면 입력 패널의 위쪽 공간을 계속
  먹는데, 그림은 몹을 고르는 순간에만 궁금한 정보다. 카드 헤더의 버튼으로 열고,
  **높이를 고정한 상자**에 넣는다 — 몹마다 그림 크기가 달라서(달팽이 ~ 보스) 크기에
  맞춰 늘리면 몹을 바꿀 때마다 아래 입력칸이 통째로 밀린다. 한 번 열면 몹을 바꿔도
  열린 채로 둬서 여러 몹을 훑어볼 때 다시 누르지 않게 한다.
  **주소는 하나로 안 된다** — 나는 몹은 `stand`가 없고 `fly`뿐이며(다크 와이번 · 카오스
  푸코 계열), 카오스 자쿰 16종은 기준 버전 v83에 아예 없어서 v88에서만 그려진다.
  `MonsterSprite`의 `SPRITE_SOURCES`가 `83/stand -> 83/fly -> 88/stand -> 88/fly` 순으로
  내려가며 시도하고, 다 실패했을 때만 "그림을 불러오지 못했다"를 띄운다.
  **기준 버전이 항상 앞**이라야 v83에 있는 몹이 v88 그림을 받아 가지 않는다.
- **피격 데미지는 몸박 · 물리 공격 · 마법 공격을 한 줄씩 따로 세운다.** 셋은 공격력도
  판정식도 다른 별개의 값이라 하나로 합치면 어느 쪽도 못 맞힌다. 예전에 물리를 몸박
  하나로 묶었다가, 화면값보다 큰 데미지를 맞았다는 제보로 고쳤다. 줄 = 피격 경로,
  열 = 공격업 단계인 표라 몹이 공격을 몇 개 갖고 있든 줄만 늘어난다.
  공격력이 다른 물리 공격이 여럿이면 번호를 붙여 **전부** 세운다.
  **라벨에 "스킬"을 쓰지 않는다** — 원작에서 몹 스킬(`MobSkill`)은 데미지가 없는
  상태이상·버프·소환 전용이라 이름이 충돌한다.
- **물리 / 마법 회피 확률은 같은 크기로 나란히 놓는다** (마법 공격이 없는 몹이면 마법
  쪽은 빼고 물리만 남긴다). 원작 판정식부터 다른 별개의
  값이라 어느 한쪽을 큰 숫자로 올리고 나머지를 각주로 미는 순간 "마법은 부속"이라는
  잘못된 인상을 준다. 두 열이 라벨 · 숫자 · 막대 · 회피율 1당 증가폭까지 같은 구조를 쓴다.
- **페이크는 공격 스킬이 아니라서 스킬 패널이 아니라 캐릭터 패널에 있다.** 데미지에
  영향을 주지 않고 회피율과 같은 자리에서 회피 확률만 움직인다. 이미 긴 스킬 패널에
  넣으면 공격 스킬 사이에 성격이 다른 항목이 끼어든다.
- **계산 모델을 켜고 끄는 옵션은 그 자리에서 무슨 일이 일어나는지 설명한다.**
  난수 순환 토글이 여기 해당한다. 유저가 "이게 뭔지" 검색하러 나가지 않아도 되게
  토글 옆에 왜 그렇게 되는지와 방컷에 어떤 방향으로 영향을 주는지를 같이 적는다.
  이 토글은 트리플 스로우일 때만 뜬다. 다른 스킬에서는 계산이 달라지지 않아서
  띄워 봐야 혼란만 준다.

## 몬스터 이동 히스토리

소환 대상 이름을 눌러 넘어간 것은 **브라우저 뒤로가기로 되돌아온다**
(`hooks/useMonsterHistory.ts`). URL에는 아무것도 싣지 않는다 — `pushState`에 주소를
넘기지 않으면 지금 주소 그대로 항목만 하나 쌓인다. 그래서 정적 배포(GitHub Pages)에
라우팅을 붙이거나 계산기 상태를 쿼리스트링으로 옮기지 않고도 뒤로가기가 살아난다.

- **항목을 쌓는 것은 소환 링크뿐이다.** 드롭다운 선택까지 쌓으면 몹을 여러 개 훑어본 뒤
  뒤로가기를 수십 번 눌러야 페이지를 벗어나게 된다. "링크를 눌러 갔으니 뒤로가기로
  돌아온다"까지만 지킨다.
- 지금 보고 있는 몹은 `replaceState`로 **항상 현재 항목에 적어 둔다.** 이걸 빼면
  드롭다운으로 고른 몹이 항목에 반영되지 않아서, 소환 링크를 눌렀다가 돌아왔을 때
  엉뚱한 몹으로 돌아간다.

### Next.js App Router와 같이 쓸 때 (안 지키면 새로고침이 난다)

App Router는 히스토리 항목에 자기 표식(`__NA`)과 라우터 트리를 넣어 두고,
**표식이 없는 항목으로 되돌아가면 `window.location.reload()`로 페이지를 통째로 다시
띄운다**(`app-router.js`의 `onPopState`). 그러면 뒤로가기 한 번에 계산기 입력이 전부
초기화된다.

Next가 `pushState` / `replaceState`를 감싸서 표식을 옮겨 주긴 하지만, **그 감싸기는
`AppRouter`의 효과라서 자식인 이 훅의 첫 `replaceState`보다 늦게 걸린다**(React는 자식
효과를 먼저 돌린다). 그래서 훅이 직접 기존 state를 펼쳐서 합친다.

```ts
window.history.pushState({ ...window.history.state, monsterId }, '');
```

실제로 이걸 빼고 만들었더니 뒤로가기가 "이전 몹으로 돌아가기"가 아니라 "페이지 새로고침"이
됐다. 히스토리를 만지는 코드를 추가할 때 같은 방식으로 합쳐야 한다.

## 방컷 확률 계산

`damageCalculator.ts`의 `calculateKillProbabilitiesWithinNHits`가 핵심이다.
"누적 데미지 -> 확률" 배열을 만들고 몬스터 HP를 흡수 상태로 둔 뒤,
스킬 1회 분포를 FFT로 반복 컨볼루션해 `dist[HP]`(= N방 안에 죽을 누적 확률)를 읽는다.

계산에 반영한 게임 스펙:
- **트리플 스로우 3라인은 서로 독립이 아니다.** 원작의 난수 순환 때문에 한 라인의
  데미지 난수가 다른 라인의 크리티컬을 결정한다. 아래 "난수 순환" 절 참고.
- 다단히트(럭키 세븐 2타, 트리플 스로우 3타)의 명중 판정은 타격별로 독립
- 쉐도우 파트너는 본체 데미지 확정 후 그 고정 비율(만렙 50%, 내림)을 그대로 따라간다
  (크리티컬 여부도 본체를 따르므로 따로 굴리지 않는다)
- 럭키 세븐과 트리플 스로우는 표창 숙련도를 무시하고 LUK만으로 데미지가 정해진다
- 크리티컬 데미지는 합연산이지만, 샤프 아이즈만 "크리티컬 데미지 40% 증가"가
  +40%p가 아니라 +140%p로 적용된다.
  Skill.wz 3121002의 `y`가 40이 아니라 **111~140**이고, 서버가
  `nSharpEyes_ = (x << 8) | y`로 싸서 넘긴 값을 `nCriticalAttackParam += (nSharpEyes & 0xFF)`로
  그대로 더하기 때문이다. 크리티컬 스로우가 0레벨이면 대신 `y + 100`이 들어간다(= +140%p).
- 데미지 라인 하나는 `[MIN_DAMAGE_PER_LINE, MAX_DAMAGE_PER_LINE]` = `[1, 199999]`로 잘린 뒤
  정수로 절삭된다. 원작은 상한이 99999인데 메이플랜드에서 199999로 올라갔다.
  방어력에 눌려 음수가 나와도 0이 아니라 1이 들어가고, **쉐도우 파트너 타격도 같은 클램프를
  받는다**(본체가 1이면 파트너도 0이 아니라 1). 본체가 하한에 눌리는 구간에서는 파트너 몫이
  타격당 데미지를 두 배로 만들어서, 빠뜨리면 방컷 확률이 0.4%p까지 어긋난다.
- **크리티컬 가산항은 스킬% 적용 전 값을 정수화해서 쓴다.**

  ```
  damage *= 스킬damage * 0.01
  damage += (critParam - 100) * 0.01 * (int)highDamage   // highDamage = 스킬% 적용 전
  ```

  그래서 크리티컬 라인은 `trunc(스킬배율 * d + 크리가산 * trunc(d))`라는 **계단**이다
  (`damageLineValue`). d 한 칸마다 값이 `스킬배율 + 크리가산`씩 오르는데 칸 안에서는
  `스킬배율`만큼만 훑으므로, 가산 배율만큼 **닿지 않는 정수 데미지가 생긴다.**
  트리플 스로우 30 + 크리티컬 스로우 30(1.5 + 1.0)이면 2500~2503은 나오는데 2504는 안 나온다.
  단순히 배율을 곱하면 크리티컬 최댓값이 1~2 높게 나온다.

### 몹 방어업을 켜면 시전 분포가 두 벌이 된다

몹 방어업(`MobSkill` 102 / 112)이 걸린 동안은 내 데미지가 그 배율로 줄어든다.
원작 `CalcDamage::PDamage`가 곱하는 자리가 중요하다.

```
damage += (critParam - 100) * 0.01 * (int)highDamage;   // 크리티컬 가산
if (nSkillID != 사크리파이스 && ms->nPGuardUp_)
    damage *= (double)ms->nPGuardUp_ * 0.01;            // ← 방어업
// 쉐도우 파트너는 이 값에서 파생되고, 클램프와 절삭은 그 뒤다
```

- **크리티컬 계단 전체가 배율만큼 눌린다.** 라인 값이
  `trunc(배율 * (스킬배율 * d + 크리가산 * trunc(d)))`가 되므로, 절삭을 먼저 하고
  곱하면 값이 어긋난다.
- **쉐도우 파트너 몫도 같이 줄어든다.** 파트너는 줄어든 본체 값에서 나온다.
- 베놈은 몹 스탯을 직접 깎는 경로라 방어업의 영향을 받지 않는다.

계산은 **시전 1회 분포를 두 벌 만들어** 처리한다. 걸리기 전(배율 1)과 걸린 뒤(배율 x)의
분포가 아예 다르므로, N방까지 누적할 때 방어업이 걸리는 시전부터 반대쪽 스펙트럼으로
컨볼루션한다. 같은 분포를 반복해서 곱하던 구조에서 **"몇 번째 시전이냐"에 따라 곱할
스펙트럼만 고르는 것**으로 바뀐 것이라, FFT 파이프라인 자체는 그대로다.

방어업이 없으면(배율 100%) 두 번째 분포를 아예 만들지 않는다.
참조 DP와 몬테카를로(둘 다 방어업을 태워 확장했다)로 대조해 검증한다.

### 라인 데미지 분포는 균등이 아니라 사다리꼴이다

원작 라인 하나는 이렇게 만들어진다.

```
라인 = trunc(clamp(1, 199999, 스탯롤 - 방어롤))
  스탯롤 ~ 균등, 폭 alpha
  방어롤 ~ U[PDD*0.5, PDD*0.6] * 배율, 폭 beta = PDD * 0.1 * 배율
  둘은 서로 독립이다
```

균등분포 두 개의 차라서 **사다리꼴**이고, 지지구간만 `[min, max]`로 같다.
양 끝만 맞춘 균등분포로 보면 두 가지가 동시에 틀어진다.

- **모양**: 방어력 난수 폭이 데미지 폭에서 차지하는 비율만큼 어긋난다.
  비율이 14%면 방컷 확률이 2.98%p, 42%면 36.4%p까지 벌어졌다.
- **클램프에 눌린 덩어리**: 하한에 눌리는 구간의 데미지가 전부 1로 뭉치는데
  균등분포에는 그 덩어리가 들어갈 자리가 없다. 질량이 구간 전체로 퍼져
  데미지를 과대평가한다(실측 평균 1.14배, 방컷 확률 최대 98%p).

그래서 `calculateKillProbabilitiesWithinNHits`에는 **클램프 전 실수 지지구간과
방어력 난수 폭**(`DamageRangeInput`)을 넘긴다. `trapezoidCdf`가 닫힌 식으로 CDF를 주고,
`addDamageSlice`가 거기서 정수 절삭과 클램프를 한 번에 처리한다.

```
데미지 1    <- 데미지가 2 미만인 모든 경우 (클램프와 절삭이 겹친다)
데미지 d    <- [d, d + 1)
데미지 상한  <- 상한 이상인 모든 경우
```

- 클램프와 쉐도우 파트너 계산은 반드시 **HP 축소 전 원본 값**에서 끝내야 한다.
  축소된 눈금에 하한 1을 강제하면 1 눈금이 원본 `1/damageScale` 데미지라 부풀어 오른다.
- 화면에 표시하는 정수 범위는 `calculateDamage`가 따로 만든다(`toDamageLine`).
- 난수 순환이 스탯 롤 축을 자르는 것도 여기서 정확히 처리된다.
  구간을 자르면 스탯 롤 폭만 줄고 방어 롤 폭은 그대로라, 같은 닫힌 식을 다시 쓰면 된다.
  전확률 공식이 기계 정밀도(1e-16)로 성립하는 것을 테스트로 고정해 뒀다.

HP가 `MAX_HP_RESOLUTION`(16383)을 넘으면 HP와 데미지를 같은 비율로 축소해 계산한다.
이 값은 FFT 길이가 32768을 넘지 않는 최대치이며, 축소로 생기는 오차는 실측 0.1%p 이하다.
표시용 데미지 범위는 반드시 내림한 정수 데미지에서 유도해 확률 계산과 어긋나지 않게 한다.

## 난수 순환 (트리플 스로우의 타격 간 상관)

원작은 공격 1회당 난수를 **7칸만 뽑아 돌려 쓴다**(`aRandom[nRndIdx++ % 7]`).
타격당 여러 개를 소비하므로 타격이 둘만 넘어가도 같은 난수가 재활용되고,
그 결과 **한 라인의 데미지 난수가 다른 라인의 크리티컬 판정을 그대로 결정한다.**

구현체 네 곳이 전부 7칸으로 같다 — `mnwvs077`(v0.77) / `Rebirth95.Server`(`RND_SIZE`) /
`iw2d/kinoko` / `Kaioru/Edelstein`. 특히 kinoko는 쓰지도 않는 난수까지 `counter++`로
세어 가며 클라이언트와 인덱스를 맞춘다. 관측되지 않는 동작이면 그렇게 짤 이유가 없다.

### 실측으로 확정한 모델

메이플랜드 실측 **40시전(듀얼 비틀, 쉐도우 파트너 OFF 27 + ON 13) 40/40**.

```
트리플 스로우 1시전 = 라인 A + 라인 B + 라인 C
  A : 데미지 난수, 크리티컬 난수 모두 독립
  B : 데미지 난수 u, 크리티컬 난수 독립
  C : 데미지 난수 독립, 크리티컬 = (u < 크리티컬 확률)
```

- 임계값이 정확히 크리티컬 확률이다. 경계 ±3%p 안쪽 표본 4건이 양방향으로 정확히 갈렸다.
- 쉐도우 파트너 ON/OFF와 무관하다. 본체 3타가 먼저 처리되고 파트너 라인은 뒤에 붙으며,
  파트너의 크리티컬은 본체 값으로 덮어써지기 때문이다.
- 관측 가능한 9개 짝(데미지 난수 i -> 크리 j)을 전수로 훑어 **딱 하나만** 살아남았다.
  나머지 8개는 15~23건씩 반례가 나왔다. 데미지끼리(|r| <= 0.21), 크리끼리도 상관이 없다.
- **평균은 안 바뀌고 분산만 준다.** 시전 1회 총합의 표준편차가 약 15% 줄어든다.
  방컷 확률로는 1.6~3.5%p 움직인다. 부호가 갈리는 기준은 **1시전 평균이 아니라
  `N * 1시전 평균`(= N방까지의 누적 평균)** 이다. 몹 HP가 그보다 낮으면 N방 컷 확률이
  올라가고 높으면 내려간다. 편차만 줄어드는 변화라 기준선이 누적 평균인 게 당연한데,
  예전 문구가 이걸 "평균 데미지"로 적어서 "HP가 1시전 평균보다 높은데 왜 오르냐"는
  혼동을 만들었다. HP 스윕으로 확인한 교차점은 비율 1.0~1.05 사이다
  (분포가 오른쪽으로 치우쳐 있어 정확히 1.0은 아니다).

`rngCyclingEnabled`(기본 ON)로 켜고 끌 수 있다. 유저가 차이를 직접 보게 하려고 남긴 옵션이라
없애지 말 것. 트리플 스로우가 아니면 계산에 영향이 없다.

### 구현

`calculateKillProbabilitiesWithinNHits`가 시전 1회 분포를 이렇게 조립한다.

```
P(1시전) = A ⊛ [ p · (B|난수<p ⊛ C=크리) + (1-p) · (B|난수>=p ⊛ C=일반) ]
```

상관은 **시전 1회 안에서만** 생기고 시전 간에는 여전히 독립이라,
그 위의 FFT 반복 컨볼루션 구조는 그대로 쓴다. `buildHitDist(uFrom, uTo, 일반가중치, 크리가중치)`가
데미지 난수 구간을 잘라 분포를 만드는 단일 출처다.

원작 엔진을 그대로 옮긴 몬테카를로 대비 최대 오차 0.30%p(HP 축소·샤프 아이즈 포함).

### 총 데미지 범위도 같이 좁아진다

화면에 뜨는 총 데미지 범위(`calculateTotalDamageRange`)도 이 결합을 반영한다.
**"라인 하나의 극값 x 타격 수"는 결합이 걸리면 도달할 수 없는 값이다.**

- **최솟값이 올라간다.** 3라인이 모두 일반이려면 C가 일반 -> `u >= p` -> B의 데미지
  난수가 최소일 수 없다. 반대로 B를 최소로 두면 C가 강제로 크리티컬이 된다.
- **최댓값이 내려간다.** 3라인이 모두 크리티컬이려면 C가 크리티컬 -> `u < p` -> B의
  데미지 난수가 상위 구간에 못 간다. B를 최대로 두면 C가 일반이 된다.

두 분기(`u < p` / `u >= p`)의 극값을 각각 만들어 비교한다. 라인 값이 d에 대해 단조
증가하므로 극값은 구간 끝에서 나오고, **u가 자르는 것은 스탯 롤 축뿐이라 방어 롤 폭은
그대로 남는다** — 자르는 지점이 `min + 스탯롤폭 * p`와 거기에 방어 롤 폭을 더한 값이다.

실제 세팅(트스30 + 크스30, 크리 50%, 쉐파 ON)에서 32,277~110,745가
37,926~101,328로 바뀐다. **기댓값은 그대로다** — 결합은 난수의 결합분포만 바꾸고
각 난수의 주변부 분포는 건드리지 않는다.

결합 여부 판정은 `isRngCyclingCoupled`가 단일 출처다. 방컷 확률과 총 데미지 범위가
서로 다른 조건을 보면 토글을 켰을 때 한쪽만 움직여서 값이 어긋난 것처럼 보인다.

### 아직 확정 못 한 것

- **화면에 뜨는 데미지 숫자의 순서가 실제 타격 순서인지.** 실측은 "아래에서부터" 기록했다.
  아래가 첫 타격이든 마지막 타격이든 3라인을 더할 뿐이라 계산 결과는 같아서 실용상 무관하다.
  다만 어느 쪽이든 **첫 번째로 처리되는 타격만 난수 소비 개수가 다르다**는 것은 확정이다
  (소비 개수가 일정하면 결합이 2개 나와야 하는데 1개뿐이다).
- **럭키 세븐**: 타격이 2개뿐이라 결합이 있을 수도 없을 수도 있는데, 실측 캐릭터가 스킬을
  안 찍어서 못 쟀다. 현재는 독립으로 둔다. 재게 되면 "아래 줄 데미지 난수 < 크리확률
  <=> 위 줄 크리" 하나만 보면 된다.

### 몹·상황에 따라 앨리어싱이 달라지는 조건

난수 소비 개수가 바뀌면 어느 난수끼리 겹치는지가 통째로 달라진다.
`CalcDamage::PDamage`에서 소비가 조건부인 곳은 다음뿐이다.

| 조건 | 소비 변화 | 비고 |
| --- | --- | --- |
| 명중 실패 | 그 타격이 2개만 소비하고 `continue` | **명중률 100%가 아니면 패턴이 흐트러진다** |
| 물리 무효(`nPImmune_`) 몹 | 1개만 소비하고 `continue` | 프리셋에 해당 몹 0종 |
| 무적(`bInvincible`) 몹 | 0개 | |
| 캐릭터가 어둠(`nDarkness_`) 상태 | 타격당 +1 | 몹이 거는 디버프라 사실상 몹 의존 |
| 크리티컬 스로우 0 + 샤프 아이즈 OFF | 크리 난수를 안 뽑음 | 크리가 아예 없으니 무관 |
| 닌자 스톰 / 쉐도우 메소 / 어쌔신네이션 | 추가·누락 있음 | 이 계산기가 다루는 스킬이 아님 |

**바뀌지 않는 것**: 방어력, HP, 레벨, 회피, 경험치, 속성 저항, 보스 여부(v0.77 기준.
v95에는 보스용 난수가 하나 더 있다), 언데드. 실측에 쓴 듀얼 비틀은 이 중 어디에도
해당하지 않는 평범한 몹이다(레벨 76 / 방어력 400 / 회피 27, 비보스, 물리 저항 없음).

따라서 **명중률 100%인 일반 몹이면 몹이 달라도 같은 패턴**이고,
명중률이 100% 미만이면 빗나간 타격이 인덱스를 밀어서 결합이 시전마다 달라진다.
계산기는 이 경우를 모델링하지 않는다(명중률 공식 자체가 이미 근사라 같이 묶어 둔다).

> 참고: 데미지 범위만으로 역산한 몹 방어력이 399.4~401.1이었는데
> 듀얼 비틀의 실제 방어력이 정확히 400이다. 역산 방법이 맞다는 독립적 확인이다.

### 원작과 다르다고 확인됐지만 아직 반영하지 않은 것

`CalcDamage::PDamage` 대조에서 나온 차이다.

- **명중률 공식**: 원작은 `a = min(999, ACC) * 100 / (max(0, 몹레벨 - 캐릭레벨) * 10 + 255)`를
  구하고 `U[0.7a, 1.3a] >= 몹회피`면 명중이다. 곧 `P = clamp((1.3a - EVA) / (0.6a), 0, 1)`.
  `calculateHitProbability`가 쓰는 커뮤니티 근사식은 ACC에 선형인데 원작은 1/ACC에 선형이고,
  레벨 페널티도 절반 이하라 명중이 애매한 구간에서 최대 45%p 차이가 난다.
  명중률 100%면 양쪽이 같아서 실사용에는 거의 안 드러난다.
원작 엔진을 그대로 옮긴 몬테카를로 대비 남은 오차는 **어느 조건에서도 0.03%p 이하**다.
남은 근사는 HP 축소(성긴 눈금)와 참조 몬테카를로의 표본 오차뿐이다.

## 넉백 확률

몹이 뒤로 밀리는 판정이다. `damageCalculator.ts`의 `calculateKnockbackProbability`가
"시전 1회에 **타격 하나라도** 몹의 넉백 수치를 넘길 확률"을 낸다.
넉백 수치는 원작 `Mob.wz`의 `info/pushed`이고 프리셋의 `minimumPushDamage`다
(438종 전부 그 몹의 WZ 값과 일치하는 것을 전수 확인했다).

**판정 단위가 시전 총합이 아니라 라인 하나라는 것이 이 계산의 전제다.**
유출 서버 코드에는 넉백 판정 자체가 없어서 — `mnwvs077`의 `MobTemplate.cpp`는
`pushed`를 읽어 `m_nPushed`에 넣어 둘 뿐 어디서도 쓰지 않는다 — 페이크·피격 데미지와
같이 **클라이언트가 판정하는 값**이다. 그래서 이 단위는 코드로 확정한 것이 아니라
정해 놓고 계산하는 기준이다.

### 계산

```
P(넉백) = 1 - P(모든 라인이 넉백 수치 미만)
```

- **라인 값이 데미지 난수 d에 대해 단조 증가**하므로 "넉백 수치를 넘기는 d"는 위쪽 꼬리
  하나로 뭉친다. 그 경계 d를 이분 탐색으로 찾아 `trapezoidCdf`를 한 번 읽으면 끝난다
  (`lineBelowProbability`). 방컷 확률이 쓰는 사다리꼴 CDF와 `damageLineValue`를 그대로
  재사용하므로 크리티컬 계단과 클램프가 자동으로 반영된다.
- **쉐도우 파트너 라인은 보지 않는다.** 파트너는 본체의 확정 데미지에 1보다 작은 비율을
  곱해 내림한 값이라 **항상 본체 이하**이고, 본체가 하한 1에 눌린 경우에도 파트너 역시
  1이다. 본체가 못 넘긴 넉백을 파트너가 넘기는 경우는 존재하지 않는다.
  (몬테카를로는 파트너 라인도 같이 굴려서 이 논증까지 검산한다.)
- **빗나간 타격에는 데미지 라인이 없으므로 넉백도 없다.** 명중 확률이 그대로 들어간다.
- **난수 순환이 걸리면 세 라인이 독립이 아니다.** 방컷 확률과 같은 분해를 쓴다.

  ```
  P(넉백 없음) = A * [ p * (B|난수<p) * (C=크리) + (1-p) * (B|난수>=p) * (C=일반) ]
  ```

  결합 여부 판정은 `isRngCyclingCoupled`를 같이 쓴다.
- **몹 방어업은 반영하지 않는다.** 화면의 총 데미지 범위와 같은 기준(안 걸린 상태)이다.
  둘 중 하나만 방어업을 태우면 같은 데미지에서 나온 두 값이 서로 어긋난 것처럼 보인다.

원작 엔진을 그대로 옮긴 몬테카를로(200만 회, 시드 3개) 대비 최대 오차 0.05%p로
전부 표본 오차 수준이다. 단일 라인은 수치적분과 소수 넷째 자리까지 일치한다.

### 이동 능력이 없는 몹은 넉백 수치와 무관하게 안 밀린다

자쿰처럼 자리에 고정된 몹은 넉백 수치를 넘겨도 밀리지 않는다. 자쿰의 `pushed`는
50000이라 어지간해서는 못 넘기지만 **넘길 수 있는 세팅이 실제로 존재**하므로,
수치만 보고 계산하면 "밀린다"는 틀린 값이 나온다.

가르는 근거는 원작 `MobTemplate::RegisterMob`이다.

```cpp
bool bFly = (info["fly"] != empty);
bool bMove = (info["move"] != empty);
bool bJump = (info["jump"] != empty);
if (bFly)        m_nMoveAbility = 3;
else if (bJump)  m_nMoveAbility = 2;
else             m_nMoveAbility = bMove ? 1 : 0;
```

`Mob.wz`의 몹 루트에 `fly` / `jump` / `move` 노드가 하나도 없으면 이동 능력이 0이다.
프리셋 438종을 전수 확인해 **63종**이 여기 해당했다(`cannotMove`). v079 · v083이 완전히
일치한다. 자쿰 본체 3종 + 자쿰팔 8종(카오스 자쿰도 11종 전부 같다), 혼테일 파트 10종,
핑크빈의 동상 5종, 피아누스 2종,
네펜데스 계열, 가시덤불, 인형자판기 · 포장마차 같은 고정 오브젝트들이다.
애니메이션을 다른 몹에서 빌려 오는 몹(`info/link`)은 링크 대상 노드로 판정한다
(페페 · 네스트 골렘 2종이 여기 해당하고 둘 다 이동 가능이다).

- 이동 불가 몹은 확률을 0으로 두고 화면에도 "넉백 불가"로 적는다. 0%로만 적으면
  "데미지를 더 올리면 밀 수 있다"로 읽힌다.
- 몬스터 패널의 칩도 `넉백 {수치}` 대신 `넉백 불가`로 갈아 끼운다.

### 확인되지 않아 가정으로 둔 것

- **판정 단위(라인 하나)**. 유출 서버 코드에 판정이 없어 실측으로만 확정할 수 있다.
  라인 하나가 넉백 수치를 넘기는 세팅과 못 넘기는 세팅을 만들어 몹이 밀리는지 보면 된다.
- **"이동 불가 = 넉백 불가"**. 자쿰이 안 밀린다는 것은 게임 상식이지만, 원작이 이동
  능력으로 거르는지 다른 플래그로 거르는지는 코드로 확인하지 못했다.
- **누적 여부**. 넉백 수치에 못 미치는 데미지가 쌓여서 밀어내는 구조인지는 확인하지
  못했다. 지금은 시전마다 독립으로 본다.
- `pushed`가 아예 없는 몹(WZ에 노드가 없어 0으로 읽히는 몹)이 "아무 데미지에나 밀린다"는
  뜻인지도 확정하지 못했다. 프리셋 438종 중 2종뿐이고(제타 그레이 · 레프러콘), 어느 쪽으로 읽어도 하한 클램프가
  1이라 계산 결과는 같다.

## 회피 확률 (몹 -> 캐릭터)

방컷 확률이 "내가 몹을 몇 방에 죽이나"라면 이쪽은 "몹이 때릴 때 얼마나 흘리나"다.
`damageCalculator.ts`의 `calculateAvoidProbability`(물리) /
`calculateMagicAvoidProbability`(마법)가 담당한다. 원작 `CalcDamage::CheckPDamageMiss` /
`CheckMDamageMiss`를 그대로 옮겼고, 데미지 계산과는 난수도 경로도 겹치지 않아 독립적으로 쓴다.

**물리와 마법은 같은 공식의 변형이 아니라 아예 다른 식이다.**

| | 물리 (`CheckPDamageMiss`) | 마법 (`CheckMDamageMiss`) |
| --- | --- | --- |
| 회피율 상한 | 999 | 999 |
| 레벨 페널티 | `-(몹Lv - 캐릭Lv) / 2` (정수 나눗셈) | `-(몹Lv - 캐릭Lv)` 전액 |
| 판정 | `회피율 / (몹명중 * 4.5) * 100 > U[0, 100)` | `U[0.1 * 회피율, 회피율] >= 몹명중` |
| 클램프 | 도적 5~95% / 그 외 2~80% | 없음 (0%도 100%도 나온다) |

- 페널티를 먹여 0 이하가 되면 0으로 둔다(음수로 안 내려간다).
- **몹 명중률이 0이어도 물리는 100%가 아니다.** 원작이 0으로 나눠 무한대가 되고 그대로
  상한에 잘리기 때문에 도적 기준 95%다. 마법은 클램프가 없어 100%가 된다.
- 닫힌 식은 각각 `clamp(회피율 / (몹명중 * 4.5), 0.05, 0.95)`와
  `clamp((회피율 - 몹명중) / (0.9 * 회피율), 0, 1)`이다.

### 페이크 (쉐도우 쉬프터)

원작 Skill.wz 412.img의 `4120002`다(섀도어는 4220002, 같은 표). `prop`이 레벨과
정확히 같아서 만렙 30이면 30%다 — `data/shadowShifter.ts`에 닫힌 식으로 뒀다.

**판정은 서버가 아니라 클라이언트에서 돈다.** 유출본 세 곳 어디에도 서버 판정이 없다.

- `Rebirth95.Server`의 `CharacterCombat`은 클라이언트가 보낸 데미지가 **-1**일 때
  "그 캐릭터가 해당 스킬을 갖고 있는지"만 검증한다(4120002 / 4220002 / 1120005 / 1220006).
- `iw2d/kinoko`는 패킷에 `missSkillId`를 실어 나르며 주석으로
  `CAvatar::SetEmotion for 4120002, 4220002`라고만 적어 뒀다.
- `mnwvs077`에는 몹 -> 캐릭터 피격 처리 자체가 없다.

즉 회피(데미지 0)와 페이크(데미지 -1)는 **클라이언트가 정해서 통보하는 별개의 결과**다.

그래서 **회피와 페이크 중 무엇을 먼저 굴리는지는 유출 코드로 확정할 수 없다.**
다만 계산에는 영향이 없다. 두 판정이 독립이고 어느 쪽이든 **단독으로** 피해를 0으로
만들기 때문에 순서를 바꿔도 총 확률이 같다.

```
a + (1 - a) * p  =  p + (1 - p) * a  =  1 - (1 - a) * (1 - p)
```

`combineAvoidWithShadowShifter`가 이 식이다. 순서는 화면에 MISS가 뜨는지 분신이
뜨는지만 가른다. 회피율 1당 증가폭은 페이크를 켜면 `(1 - 페이크)`배로 줄어든다
(페이크가 먼저 걸러 낸 몫만큼 회피율 한 점의 값어치가 작아진다).

물리와 마법 모두에 걸린다 — Rebirth95에서 `-1` 처리 블록이 물리·마법 분기 **바깥**에 있다.

확인되지 않아 가정으로 둔 것:
- 페이크 난수가 회피 판정 난수와 독립이라는 것. 원작이 miss 체크용 난수 생성기
  (`m_RndForCheckDamageMiss`)를 따로 두는 데 근거한 가정이다.
- 페이크가 회피에 실패한 타격에만 굴려지는지. 위 식이 순서에 무관하므로 실용상 무관하다.

### 도적 클램프(5~95%)의 근거

원작 분기는 `bs->nJob / 100 == 4`다. 이 숫자가 해적이 아니라 **도적**이라는 것은
같은 유출본 안에서 교차확인했다 — `CalcDamage.cpp`의 마법사 분기가 `== 2`이고,
`QWUser.cpp`의 AP 요구치가 `3 또는 4 -> LUK`(궁수·도적)이며, 아대(무기 타입 33) 분기도
`== 4`를 쓴다. 이 계산기는 나이트로드 전용이라 도적 값만 넣었다.

### 유출본 사이의 차이

- `mnwvs077`은 물리 판정을 `calc > GetNormalizedCurrentRand()`로 적어 뒀는데,
  그 매크로는 `[0, 1)`을 주고 `calc`는 퍼센트(2~95)라 단위가 안 맞는다. 그대로면 항상
  회피가 된다. `Rebirth95.Server`가 같은 판정을 `get_rand(rnd, 100.0, 0.0)`으로 굴리므로
  **난수 쪽이 0~100이 맞다**고 보고 퍼센트로 맞췄다.
- 단 `Rebirth95.Server`의 `calc_evar`(`sqrt(회피) - sqrt(몹명중)`, 레벨차 5배 차감)는
  **빅뱅 이후 v95 공식**이라 쓰지 않는다. 메이플랜드는 빅뱅 전이다.
- 두 판정 모두 `mnwvs077`에서는 호출부가 없는 죽은 코드다(몹의 공격 처리가 구현돼 있지
  않다). IDA 디컴파일을 옮겨만 둔 것이라 식 자체는 원작 그대로로 본다.

### 확인되지 않아 가정으로 둔 것

- **실측이 없다.** 회피는 성공/실패만 보이고 수치가 안 떠서, 데미지처럼 역산할 수단이 없다.
  베놈·난수 순환과 달리 이 절은 전부 유출 코드 기준이다.
- **몸박(터치 데미지)이 물리 경로라는 것**은 가정이다. 원작에서 몹 공격의 물리/마법 구분은
  `MobAttackInfo`의 타입이 정하는데, 프리셋에는 그 정보가 없어서 UI는 물리를 기본으로 두고
  마법 값을 함께 적어 준다.
- **회피 불가 몹**: `Rebirth95.Server`에는 `CannotEvade` 플래그가 있는데 v0.77 쪽에서는
  확인하지 못했고 프리셋에도 없어서 반영하지 않았다.

## 피격 데미지 (몹 -> 캐릭터)

회피 확률이 "몹이 때릴 때 얼마나 흘리나"라면 이쪽은 "흘리지 못했을 때 얼마나 아픈가"다.
`damageCalculator.ts`의 `calculateHitDamageBreakdown`이 물리·마법을 한 번에 낸다.

### 유출 코드 조사 결과: 계산 방식이 하나가 아니다

**피격 데미지는 서버가 계산하지 않는다.** 클라이언트가 계산해서 통보하고 서버는
감소 버프만 빼는 구조다 — 페이크와 같다.

- `iw2d/kinoko`의 `HitHandler.handleUserHit`은 패킷의 `nDamage`를 그대로 받아
  메소 가드·매직 가드 같은 감소 버프만 빼고 HP를 깎는다.
- `Rebirth95.Server`의 `CharacterCombat.OnHit`도 같다. 검증은
  `if (nDamageInternal < -1) return;` 한 줄뿐이다.
- `mnwvs077`은 `User::OnHit`과 `User::InspectDamageMiss`가 **선언만 있고 구현이 없다.**
  함수 이름이 `Inspect`(검사)인 것 자체가 이 구조를 드러낸다.

그래서 **공식의 유일한 출처는 `mnwvs077`의 v0.77 IDA 전사본**
(`CalcDamage::PDamage/MDamage(MobStat*, MobAttackInfo*, …)`)이다.
kinoko와 Edelstein에는 몹->캐릭터 계산이 아예 없고(캐릭터->몹만 있다),
`Rebirth95.Server`에는 있지만 **빅뱅 이후 v95 공식**이라 쓸 수 없다
(`calc_mob_base_damage`가 `U[MAD, 0.85*MAD]`로 1차식이고, 방어는
`sqrt(방어력/4)` 기반 `adjust_base_defense`다. 게다가 그 MDamage는 `return 0`으로
끝나는 미완성이고 물리 쪽은 빈 스텁이다).

v0.77 안에서도 경로마다 식이 다르다.

| | 물리 `PDamage` | 마법 `MDamage` | 소환수 피격 `PDamageSummoned` |
| --- | --- | --- | --- |
| 기본 데미지 | `U[0.8*PAD, 0.85*PAD] * PAD * 0.01` | `U[0.75*MAD, 0.8*MAD] * MAD * 0.01` | `U[0.1*PAD, 0.85*PAD] * PAD * 40/(SLV+70) * 0.01` |
| 공격력 출처 | 공격별 `PADamage`(몸박은 몹 기본 PAD) | **몹 MADamage 고정** (공격 정보를 안 본다) | 몹 PAD |
| 방어 반영 | 캐릭 PDD + 표준 방어력 표 + 스탯항 + **레벨차** | 캐릭 MDD + 스탯항 x 직업계수 | 없음 |
| 클램프 | `[1, 99999]` | `[1, 99999]` | 없음 |

`nAttackIdx`가 -2(카운터) / -3(장애물) / -4(상태이상 도트)인 피격은 계산식 자체가 없다.
맵 데이터나 도트에서 온 고정값이고, `deadlyAttack` 몹 공격은 공식과 무관하게 HP를 1로 만든다.

### 구현한 식

```
물리 = trunc(clamp(1, 99999,
        U[0.8*공격력, 0.85*공격력] * 공격력 * 0.01
        - (표준차항 + (스탯항 * 0.00125 + 0.28) * 물리방어력)))

  스탯항  = trunc(INT/9 + DEX/3.5 + STR*0.4 + LUK*0.25)      // 전사가 아닌 직업
  표준차  = 물리방어력 - getStandardPhysicalDefense(레벨)
  표준차항 = 표준차 >= 0
      ? (스탯항*0.0011111 + 레벨*0.00076923 + 0.28) * 표준차 * 0.7
      : (레벨*0.0018182 + 스탯항*0.00125 + 0.28) * 표준차
        * (캐릭레벨 >= 몹레벨 ? 13/((캐릭레벨-몹레벨)+13) : 1.3)

마법 = trunc(clamp(1, 99999,
        U[0.75*마공, 0.8*마공] * 마공 * 0.01
        - (STR/7 + LUK*0.2 + DEX/6 + 마법방어력) * 0.25))     // 마법사면 0.3
```

- **데미지는 몹 공격력의 2차식이다.** 롤을 한 번 굴린 뒤 공격력을 한 번 더 곱한다.
- **난수는 이 롤 하나뿐이다.** 방어력 쪽에는 난수가 없어서 공격 데미지와 달리
  분포가 사다리꼴이 아니라 균등이고, 폭도 최댓값의 6%(물리) / 6.25%(마법)로 좁다.
  그래서 UI는 확률분포 없이 범위만 보여준다.
- **몹 공격력과 캐릭터 방어력은 둘 다 0~1999에서 잘린다.** 공격력 상한 때문에 피격은
  최대 약 34,000이라 상한 99999에는 닿지 않는다(메이플랜드가 199999로 올린 것은
  캐릭터가 주는 데미지 쪽이다).
- **물리는 방어력의 절댓값이 아니라 레벨대 표준 방어력과의 차이로 들어간다**
  (`data/standardPDD.ts`, 원작 `Base.wz`의 `StandardPDD.img`).
  표준보다 낮은 구간에서만 레벨 차이가 개입한다. 표에 없는 레벨은 **바로 아래 칸의
  값**을 그대로 쓴다 — 원작 `LoadStandardPDD`가 0~255를 훑으며 노드가 있을 때만
  값을 갱신하기 때문이다. 도적은 100레벨(331)에서 멈춘다.
  v079와 v083 WZ 값이 완전히 같은 것을 확인했다.
- **INT가 여기서만 쓰인다.** 순수 INT는 4로 고정이고(`PURE_INT`, LUK 순 스탯 역산도
  같은 가정을 쓴다) 장비로 붙은 추가분만 입력받는다. 총 INT 100 기준으로 물리 피격이
  8 정도 움직이는 크기라 크지 않지만, 하드코딩하면 근거 없는 근사가 되므로 입력으로 뒀다.
- 마법은 레벨 차이도, 표준 방어력 표도, 데미지 감소 버프도 개입하지 않는다.
  같은 몹이라도 마공이 물공보다 높으면 마법이 훨씬 아프다.

### 몹 공격은 몸박 하나가 아니다

원작 엔진은 몹의 행동을 **공격(`attack{n}`)과 스킬(`MobSkill`)로 나눈다.**
데미지를 주는 것은 공격뿐이고 스킬은 상태이상·버프·소환 전용이다
(kinoko `MobHandler.handleMobAttack`이 `MobActionType.ATTACK*`과 `SKILL*`을
갈라서, 공격이면 `conMP`만 깎고 스킬이면 `MobSkill`을 적용한다).
화면 라벨을 "물리 공격"으로 쓰고 "스킬"이라고 쓰지 않는 이유다.

원작 몹은 `attack1`, `attack2`… 마다 자기 `PADamage`를 가질 수 있고, 엔진은
`max(몹 기본 PADamage, 공격별 PADamage)`를 쓴다. **몸박만 계산하면 실제로 맞는 최댓값을
놓친다.** 프리셋 438종을 `Mob.wz`와 전수 대조해서 확인한 것들이다.

- **74종이 몸박보다 센 공격을 갖고 있다.** 블러드붐 500 -> 1100(2.2배),
  로우 다크스타 2.0배, 머쉬맘 1.5배, 예티 1.48배… 프리셋의
  프리셋의 `physicalAttackPowers`가 그 목록(몸박보다 센 값만, 오름차순)이고,
  없으면 물리는 몸박 하나뿐이라는 뜻이다. 값이 두 종류인 몹도 12종 있다
  (코니언 계열 · 신관 계열 · 듀얼 버크 · 뉴트주니어 · 카오스 자쿰팔 7 · 8). **화면은 공격마다 한 줄씩 세운다** —
  하나로 묶으면 나머지 공격에 맞았을 때 화면에 없는 숫자가 되고, 그 순간 계산기를
  못 믿게 된다.
- **마법 공격을 가진 몹은 153종뿐이다.** 마법 공격력이 0보다 큰데 마법 공격
  (`attack{n}/info/magic`)이 아예 없는 몹이 **97종** 있어서, 마공만 보고 그리면 없는
  데미지를 만들어 낸다. `hasMagicAttack`으로 가른다.
- **공격 노드에는 마법 공격력이 없다.** 438종 전수 확인 결과 `MADamage`를 가진 공격
  노드가 0개고, `magic=1`인 공격 176개도 자기 데미지 값이 없다. 즉 마법 피격은 몹의 기본
  `MADamage` 하나로 정해진다. `MobSkill.img`의 39개 스킬에도 데미지 필드가 없다
  (`hp`는 데미지가 아니라 "HP 몇 % 이하일 때 시전"이라는 조건이다).
- 프리셋 438종의 기본 `PADamage` / `MADamage`가 그 몹의 WZ 값과 **전부 일치**하는 것도
  같이 확인했다(카오스 자쿰 16종만 기준 버전이 아니라 v88이다 — 아래 프리셋 절 참고).

이 절은 실측 제보에서 나왔다. 망각의 수호대장(8200012)에게 마법 최대가 3,995인데
4,000~4,100을 맞았다는 보고였고, 그 값은 마법이 아니라 `attack2`(공격력 755)의
물리 범위(3,845~4,130)였다. 몸박(645)만 띄우고 있어서 물리로 보이지 않았던 것이다.

### 그 공격이 실제로 쓰이는지는 코드로 확정할 수 없다

**어떤 공격을 쓸지는 서버가 아니라 몹을 조종하는 클라이언트의 AI가 고른다.** 서버는
받은 인덱스로 `conMP`만 깎을 뿐이라(kinoko `handleMobAttack`), 유출 서버 코드에는 선택
로직 자체가 없다. WZ에서 확인할 수 있는 것은 "쓸 수 있는 조건"까지다.

| WZ 필드 | 뜻 |
| --- | --- |
| `conMP` | 몹 MP 소모. 프리셋 438종 중 MP가 모자라 보이는 공격은 **1건**뿐이다 — 레프러콘(9400583)의 `attack1`(공격력 1000)이 MP 0에 `conMP` 2를 요구한다. 그래도 화면에서 빼지는 않았다. 못 쓰는 공격을 띄우는 쪽보다 실제로 맞는 공격을 빠뜨리는 쪽이 위험하고, 서버 유출본은 MP를 검사하지 않고 그냥 깎는다 |
| `range` / `hit` | 공격이 닿는 사각형. 269개 공격 전부 갖고 있다(= 실제 판정이 있다) |
| `jumpAttack` | 점프 중에만 쓰는 공격 (31개) |
| `effect` / `ball` / `areaWarning` | 이펙트 · 투사체 · 예고 범위 |

결국 확정은 실측이다. 망각의 수호대장의 `attack2`(755)는 **제보된 4,000~4,100이 그
범위(3,845~4,130) 안**이라 실제로 쓰인다는 게 확인된 셈이다.

### 몹 공격업

원작 `MobSkill.img`의 **110(웨폰 어택 업) / 111(매직 어택 업)** 이다
(112 / 113은 방어업이라 피격에는 영향이 없다). `Mob::DoSkill_StateChange`가 그 레벨의
`x`를 몹 스탯 `PowerUp_` / `MagicUp_`에 넣고, `PDamage` / `MDamage`가 **클램프 직전에**
`damage *= x * 0.01`로 곱한다.

```
공격업 데미지 = trunc(clamp(1, 99999, (기본 - 감면) * 배율))
```

- **감면이 끝난 값에 곱한다.** 몹 공격력을 그 배율만큼 올려서 계산하면 값이 다르다 —
  공격력은 2차식으로 들어가는 데다 감면이 그대로 남기 때문이다.
- **절삭은 곱한 뒤 한 번만 한다.** 화면에 뜬 정수 데미지에 배율을 곱하면 1씩 어긋난다
  (498 x 1.15 = 572지만 실제로는 498.5 x 1.15 = 573이다).
- 하한 1에 눌린 데미지는 배율을 곱해도 1이다.
- 곱하는 자리가 **감면·감소 버프·메소 가드를 전부 끝낸 맨 마지막**이라, 화면 각주는
  "공격업은 정해진 데미지를 1단계 +15% · 2단계 +30% 올린다"로 적는다. 절삭만 뒤에
  남으므로 화면값과 최대 1 차이다.
- 단계별 배율은 `data/mobBuffs.ts`에 둔다. WZ의 "레벨"은 강약 단계가 아니라 몹마다
  골라 쓰는 슬롯이라 값이 겹친다 — 실제로 나오는 값은 110이 `115 / 130 / 200 / 100`,
  111이 `115 / 130 / 140 / 100`이고, 이 중 게임에서 1단계 · 2단계로 통하는 것이
  115와 130이다. v079와 v083 WZ가 같다.

### 반영하지 않은 것

원작 `PDamage`/`MDamage`에는 아래가 더 붙는다. 나이트로드가 상시로 걸고 다니는
값이 아니라 전부 0으로 뒀다.

- `ss->nInvincible_` (데미지 감소 버프) 비율 감면 — 물리에만 있다
- 메소 가드 (`GetMesoGuardReduce`)

### 확인되지 않아 가정으로 둔 것

- **실측이 없다.** 다만 회피 확률과 달리 **실측으로 검증할 수 있다** — 피격은 화면에
  숫자가 뜨고 난수가 하나뿐이라, 몇 대만 맞아 봐도 몹 공격력을 역산할 수 있다.
  듀얼 비틀 방어력을 데미지 범위에서 역산했던 것과 같은 방법이다.
- **어느 공격이 얼마나 자주 나오는지는 모른다.** 공격력이 다른 공격을 전부 세워 줄 뿐,
  빈도는 반영하지 않는다.
- **어느 몹이 공격업을 쓰는지는 `mobSkills`에 있지만, UI는 몹과 무관하게 1 · 2단계를
  나란히 보여준다.** 몹이 그 슬롯을 지금 걸고 있는지는 알 수 없어서다.
  **배율이 큰 슬롯도 실제로 쓰인다** — 레드너그(9400591)가 110 레벨 8(200% = 데미지 2배),
  키메라(8220002)가 111 레벨 5(140%)를 쓴다. 이 둘은 화면의 1 · 2단계(115 / 130)로
  최악을 못 맞힌다.
- **`nAttackIdx = -1`이 몸박인지 몹의 마법 공격인지**는 확정하지 못했다.
  `Rebirth95.Server`와 `kinoko`는 enum 이름을 `AttackIndex_Mob_Magic`으로 달아 뒀지만,
  `mnwvs077`의 `PDamage`에는 공격 정보 없이(=몹 기본 PAD로) 계산하는 분기가 있고
  v83 계열 에뮬레이터(HeavenMS)는 -1에서 파워 가드를 태운다. 물리는 PAD, 마법은 MAD라는
  갈래 자체는 어느 쪽이든 같아서 계산에는 영향이 없다.
- **meowdb의 MapleStory Classic 문서로는 교차검증할 수 없다.** 그쪽은 빅뱅 이전 원작이
  아니라 다른 빌드다 — 피격이 `공격력 * U[1.1, 1.5]`인 1차식이고, 방어는 비율식
  `1 - DEF/(DEF + 5*(레벨+40) + 1.2*Raw)`, 데미지 상한이 5천만, 실드/클로 가드 확률과
  최소 명중 8% 구제까지 있다. v0.77 식과 겹치는 부분이 없다.

## 몹 스킬

`Mob.wz`의 `info/skill`에 있는 목록이고, ID는 `MobSkill.img`의 스킬 ID다.
**몹 스킬은 데미지를 주지 않는다** — 자기(또는 주변 몹) 버프, 캐릭터 디버프, 소환만 한다
(데미지는 전부 `attack{n}` 쪽이다). ID 대응은 `mnwvs077`의 `Mob::DoSkill_StateChange` /
`DoSkill_PartizanStatChange`와 `iw2d/kinoko`의 `MobSkillType`에서 교차확인했다.

- 100번대는 자기 자신에게, 110번대는 **주변 몹 전체**에게 거는 같은 버프다.
- 프리셋 438종 중 스킬을 가진 몹은 **115종**이다.

### 계산에 반영하지 않고 경고만 하는 이유

몹 스킬은 **걸려 있는 동안에만** 효과가 있다. 상시 반영하면 안 걸린 대부분의 시간에
틀린 숫자가 되고, 무시하면 걸렸을 때 "계산기가 틀렸다"고 읽힌다. 그래서 숫자는 그대로
두고 **언제 이 숫자가 안 맞는지**를 알려 준다 — `mobSkills.ts`의 `impact`가 그 분류다.

| impact | 뜻 | 화면 |
| --- | --- | --- |
| `kill` | 방컷 확률이 나빠진다 | 몬스터 패널 칩이 `danger`, 방컷 확률 아래에 경고 한 줄 |
| `player` | 캐릭터가 불리해진다 (계산 밖) | 칩이 `crit` |
| `info` | 나이트로드에겐 영향 없음 | 기본 칩 |

`kill`로 분류한 근거는 전부 원작 코드다.

- **물리 무효(140)**: `PDamage`가 `nPImmune_`이면 데미지를 **1**로 만든다. 프리셋 24종.
- **방어업(102 / 112)**: `damage *= nPGuardUp_ * 0.01`. WZ 값이 85(-15%)와 60(-40%)이다.
  프리셋 28종인데, 그중 4종(크림슨우드 성채 보스)은 `x`가 100이라 실제로는 데미지가
  안 줄어든다. **`x`를 안 보면 효과 없는 스킬에 빨간 칩과 방컷 경고가 붙으므로**
  `getMobSkillInfo(id, x)`가 배율 100 이상이면 `info`로 낮춘다.
- **회복(114)**: 주변 몹 HP를 `x`만큼 되돌린다(500~500000). 프리셋 28종.
- **경화(142)**: `if (!ms->nHardSkin_ || abCritical[i])` — **크리티컬이 아닌 타격은
  데미지가 아예 안 들어간다.** 프리셋에는 없지만 표에는 넣어 뒀다.
- **방어력 증가(152)**, **피해 흡수(161)**도 같은 이유로 `kill`이다.

### 소환 대상

소환(200)은 스킬 레벨마다 부르는 몹이 다르고, 한 몹이 소환 스킬을 여러 개 갖기도 한다
(자쿰은 HP 구간마다 다른 몹을 부른다). 그래서 프리셋에는 그 몹이 부르는 대상을 전부
모아 `summons`로 담는다 — `{ name, count }`이고 `count`는 한 번에 나오는 마리 수다.

대상의 **몹 ID도 같이 담아서, 프리셋에 있는 몹이면 이름을 눌러 그 몹으로 넘어간다.**
소환 대상 40종 중 34종이 프리셋에 있다. 이름은 프리셋에 있으면 **프리셋 이름을 그대로**
쓴다 — 드롭다운과 소환 줄이 같은 몹을 다르게 부르면 눌러서 넘어간 뒤 잘못 눌렀다고
읽힌다. 프리셋에 없으면 `Mapleland-Discord-InfoBot`의 `data/mobs.json`(한글),
그것도 없으면 v83 `String.wz`의 영문명을 쓰고 누를 수 없는 글자로 둔다. 키메라가 부르는 둘(`9300167` / `9300168`)이 한글 목록에
없어서 영문(`Degraded Rumo` / `Reinforced Roid`)으로 뒀다 — **메이플랜드에 그 몹이
실제로 있는지는 확인하지 못했다.**

### 화면에 넣지 않은 것

`x`의 뜻이 확인된 스킬(공격업 · 방어업 · 회복 · 중독 · 슬로우 · 독 안개)만 프리셋에
수치를 담는다. 물리 무효처럼 `x`가 플래그(1)인 스킬은 숫자를 안 적는다 —
뜻 모르는 숫자를 칩에 붙이면 근거 없는 정보가 된다.

## 공격 스킬별 특성

이 계산기는 나이트로드(표창) 기준이다.

| 스킬 | 타격 수 | 데미지 계수 | 베놈 판정 | 비고 |
| --- | --- | --- | --- | --- |
| 럭키 세븐 | 2 | LUK만 사용 (숙련도·STR·DEX 무시) | O | 최소:최대 = 1:2 고정 |
| 트리플 스로우 | 3 | LUK만 사용 (숙련도·STR·DEX 무시) | O | 최소:최대 = 1:2 고정 |
| 어벤져 | 1 | 스탯 공격력 (숙련도 영향 받음) | O | 다중 타겟 |
| 드레인 | 1 | 스탯 공격력 (숙련도 영향 받음) | **X** | 유출 코드의 베놈 제외 목록에 있음 |

공통 사항:
- 쉐도우 파트너는 본체 데미지가 확정된 뒤 그 고정 비율(만렙 50%, 내림)을 따라간다.
  크리티컬 여부도 본체를 따르므로 따로 굴리지 않는다.
  다만 **베놈 발동 판정은 파트너 타격도 따로 굴린다**(판정 횟수가 2배).
- 크리티컬 데미지는 합연산이지만, 샤프 아이즈만 "크리티컬 데미지 40% 증가"가
  +40%p가 아니라 +140%p로 적용된다.
- 다단히트의 명중 판정은 타격별로 독립이다.

## 베놈

`utils/venom.ts`가 담당한다. 아래 내용은 브라질(BMS) 빅뱅 전 공식 서버 유출 코드를
디컴파일한 `CMob::OnMobStatChangeSkill`에서 확인한 것이다.

### 데미지 공식

```
S    = STR + LUK            (장비·버프 포함 최종 스탯)
v42  = floor(0.8 * S) + rand(0, S - 1)
틱뎀  = floor(mad * (DEX + 5 * v42) / 49)
```

- `mad`는 캐릭터 공격력이 아니라 **베놈 스킬 자체의 공격력**이다.
- 몹 방어력 / 레벨 차이 / 크리티컬 / 스킬 데미지% 어느 것도 영향을 주지 않는다.
  데미지 라인이 아니라 몹 스탯을 바꿔 HP를 직접 깎는 방식이기 때문이다.
- 커뮤니티에 알려진 `{18.5*(STR+LUK) + DEX*2}/100 * 스공`은 `/49`를 `/98`로 바꿔
  2배한 근사식이다. 평균만 같고 정확하지 않으므로 위 원식을 쓴다.

### 레벨별 수치 (원작 Skill.wz 412.img / 422.img, 두 직업 동일)

닫힌 식으로 정리된다. `mad = 30 + 레벨`, `prop = 12 + 2*floor((레벨-1)/3)`,
`지속시간 = 2 + floor((레벨-1)/10)`초. 만렙 30에서 mad 60 / 확률 30% / 4초.

### 발동과 중첩

- **명중한 타격마다** 독립적으로 `prop%` 판정을 굴린다.
- 중첩 조건은 `기존 누적 <= 신규 * 2`. 통과하면 합연산으로 누적되고 지속시간이 갱신된다.
  실패하면 **아무것도 바뀌지 않는다**(지속시간 갱신도 없음).
- 스택 카운터가 따로 없어서 "3중첩"은 하드코딩이 아니라 이 조건에서 나오는 결과다.
  앞선 롤이 낮게 뜨면 4중첩 이상도 가능하다.
- 이 조건 때문에 누적 스택은 `3 * 최대 롤`을 넘을 수 없다(상태 공간이 유한).

### 틱과 사망 판정

- 도트 클럭은 **부여 시점에 고정된다**. 중첩에 성공할 때마다 `m_tLastUpdateVenom`이
  그 시각으로 초기화되고, 거기서 1초 간격으로 틱이 들어간다. 자유진행 클럭이 아니다.
  드리프트도 없다(`lastUpdate += 1000 * n`).
  → **첫 틱은 부여 후 정확히 1초 뒤**다. 트리플 스로우 100회/분(0.6초 주기)이면
  첫 틱이 두 번째 공격 뒤에 떨어진다. 메이플랜드 실측으로 확인했다.
- 중첩에 성공하면 타이머가 초기화되므로, 계속 중첩되는 동안에는 틱이 뒤로 밀린다.
  이것도 실측으로 확인했다.
- 한 번 부여된 베놈은 **지속시간 + 1**개의 틱을 받고 만료되며, 만료 시 누적이 0으로 돌아간다.
  만료 시각은 `tVenom_ = tCur + tDelay + 1000 * 지속시간`인데 틱 클럭 기준점
  `m_tLastUpdateVenom`에는 `tCur`만 들어가고, `LifePool::ApplyUserAttack`이 베놈을 걸 때
  `tDelay = 1000`을 넘긴다. 그래서 틱이 한 번 더 들어간다(만렙 5틱).
  `VENOM_APPLY_DELAY_SECONDS` / `getVenomTickCount`가 이걸 담당한다.
- **베놈 데미지로는 몬스터를 죽일 수 없다.** 서버가 `max(1, HP - 틱뎀)`으로 자른다.
- 보스, 독 무효(elemAttr S1), 독 반감(S2) 몬스터에는 아예 걸리지 않는다.
- **틱 1회 데미지는 30000에서 잘린다**(`VENOM_TICK_DAMAGE_CAP`).
  이건 원작 유출 코드에 **없는** 제한이다. `nVenom_`은 int이고 누적에 제한이 없다.
  메이플랜드 실측에서 확인된 메이플랜드 고유 사양이라 별도 상수로 뒀다.
  중첩 판정(`기존 <= 신규 * 2`)에는 상한을 적용하지 않고 실제로 들어가는
  데미지만 자른다. 상한이 내부 누적값까지 자르는지는 확인되지 않았다.

### 실측으로 확인한 것

- 첫 틱은 부여 후 1초 뒤다. 트스 0.6초 주기에서 첫 틱은 **두 번째 트스 딜 이후**에 박힌다.
  화면상으로는 첫 공격과 두 번째 공격 사이처럼 보이지만 실제 HP는 그렇게 깎이지 않는다.
- 트스 1타 + 트스 2타 + 베놈 1틱이 몬스터 HP보다 커도 죽지 않는다.
  반대로 베놈 틱이 표시되면서 2방에 죽은 경우는 전부 베놈을 빼고도 트스 2방이 HP를 넘겼다.
  → 베놈이 몬스터를 죽이지 못한다는 것이 실제로 확인됐다.
- 중첩에 성공하면 틱 타이머가 초기화된다.

### 판정 횟수 — 유출 코드와 실측이 갈리는 곳

**판정은 타격마다 한 번씩 굴린다.** 트리플 스로우는 3회, 트리플 스로우 + 쉐도우 파트너는
6회다(`rollsPerUse = getHitCount * (파트너 ? 2 : 1)`). **메이플랜드 실측 기준**이다.

유출 코드는 이것과 다르게 보인다. `LifePool::ApplyUserAttack`에서 타격 루프는 데미지를
`liDamageSum`에 더하기만 하고, `OnMobStatChangeSkill`은 루프 **바깥**에서 몹 하나당 한 번
호출된다(함수가 `nDamageSum`을 받는 것도 같은 얘기다). 그대로면 공격당 1회여야 한다.

실측이 우선이므로 타격당 1회를 유지한다. **유출 코드만 보고 되돌리지 말 것.**
메이플랜드가 이 부분을 바꿨거나, 유출본 계열과 실제 서버 구현이 다른 것으로 본다.

### 확인되지 않아 가정으로 둔 것

- **빗나간 타격의 판정 여부**: 실사용 대부분이 명중률 100%라 단순화를 위해
  명중과 무관하게 항상 굴리는 것으로 뒀다.

### 계산 구조 (FFT 정확도를 지키는 방법)

베놈 누적 데미지 `W`는 스택 상태 때문에 iid 합이 아니라 마르코프 과정이다.
하지만 베놈이 굴리는 난수는 공격 데미지 난수와 **완전히 독립**이라 분리할 수 있다.

1. `calculateVenomSurvivals`가 `(부여 이후 경과한 공격 수, 양자화된 누적 스택,
   누적 베놈 데미지)` 결합분포를 굴려 시전 N회 시점의 `P(W_N >= t)`를 구한다.
   중첩은 공격할 때만 일어나므로 부여 시각은 항상 공격 시각이고, 덕분에
   "부여 이후 경과한 공격 수"만 알면 그 구간에 틱이 몇 번 들어가는지가 정해진다.
2. 공격 쪽 FFT 파이프라인은 손대지 않고, 마지막에 내적 한 번으로 합류시킨다.
   `P(N방 안 사망) = dist[HP] + Σ dist[i] * P(W_N >= HP - i)`

"베놈은 못 죽인다"는 제약은 다음 정리로 근사 없이 반영된다.

> N방 안에 죽는다 ⟺ `누적 공격뎀 + 누적 베놈뎀 >= HP` 이고,
> HP를 넘긴 시점 이후에 데미지 1 이상인 공격이 한 번이라도 있었다

뒤 조건은 명중률 100%면 항상 참이라 앞 조건만 보면 되고,
명중률이 낮을 때의 오차는 `(1 - 명중률)^타격수`로 묶인다.

유일한 근사는 누적 스택 양자화(`VENOM_STACK_LEVELS`)다. 게이트(`a <= 2v`) 판정은
반드시 **양자화 전의 정확한 롤 값**으로 해야 한다. 양자화한 값으로 판정하면
"올림된 롤이 게이트를 더 잘 통과"하는 상관이 생겨 중첩 데미지가 계통적으로
+0.4% 부풀어 오른다.

96단계 기준 몬테카를로 400만 회 대비 최대 오차는 0.01%p다.
HP 축소가 걸리는 구간에서는 0.13%p까지 벌어지지만, 같은 조건에서 베놈을 끄고 재면
축소 오차만으로 0.22%p가 나온다. 즉 베놈 쪽이 더하는 오차는 사실상 없다.

## 원작 유출 자료

베놈처럼 원작 동작을 다시 확인해야 할 때 참고할 곳들이다.
GMS/KMS 버전 차이가 있을 수 있으니 수치는 항상 교차검증한다.

| 자료 | 용도 | 특징 |
| --- | --- | --- |
| [`CMob__OnMobStatChangeSkill.txt`](https://drive.google.com/file/d/1AipXS7vutfhrNALtJxvTcQjBZgvIrgQU/view) | 몹 상태이상 스킬 전반 | 브라질(BMS) 빅뱅 전 공식 서버를 IDA로 디컴파일한 **진짜 넥슨 코드**. 베놈·쇼다운·닌자 앰부시·독 안개 등의 적용 조건과 공식이 전부 들어 있다. 신뢰도 최상 |
| [tnsc4502/mnwvs077](https://github.com/tnsc4502/mnwvs077) | 도트 틱 처리, 공격 처리 흐름, 회피 판정과 **피격 데미지**(`WvsGame/CalcDamage.cpp`의 `CheckPDamageMiss` / `CheckMDamageMiss` / `PDamage` / `MDamage`) | 원작 v0.77 구조를 그대로 옮긴 C++ 재구현. `WvsGame/Mob.cpp`의 `UpdateMobStatChange`가 1초 틱과 HP 하한 1을, `WvsGame/LifePool.cpp`의 `ApplyUserAttack`이 베놈이 붙는 스킬 목록을 보여준다. **재구현이라 세부는 원본과 다를 수 있다**(중첩 조건이 원본과 다르게 단순화돼 있음) |
| [67-6f-64/Rebirth95.Server](https://github.com/67-6f-64/Rebirth95.Server) | 대조용 | v95 기반 C# 재구현. 도트 틱을 몹 단위 자유진행 1초 클럭으로 돌린다. mnwvs077과 구조가 달라 두 방식을 비교할 때 쓴다. 회피 판정의 난수 범위(0~100)를 확인한 곳이기도 한데, **공식 자체는 빅뱅 이후 것**이라 그대로 가져오면 안 된다 |
| [iw2d/kinoko](https://github.com/iw2d/kinoko) | 난수 소비 순서 대조 | `world/user/stat/CalcDamage.java`. 쓰지 않는 난수까지 `counter++`로 세어 클라이언트와 인덱스를 맞춘다. 7칸 순환이 실재한다는 강한 증거 |
| [Kaioru/Edelstein](https://github.com/Kaioru/Edelstein) | 난수 소비 순서 대조 | C# 재구현. `RndSize = 7` + `Rotational<uint>` |
| [maplestory.io 몹 API](https://maplestory.io/api/gms/83/mob/100100) | **몹 수치 1차 출처** | `GMS/83`이 기준 버전이다. `/mob/`로 그 버전의 몹 목록을, `/mob/{id}`로 `info` 전 항목과 출현 맵(`foundAt`)을 준다. 아래 "기준 버전" 절 참고 |
| [mrzhqiang/ms079](https://github.com/mrzhqiang/ms079) | **API가 안 주는 노드의 1순위 덤프** | v0.79 기준 `wz/Mob.wz/*.img.xml`. API v83과 **359종 전 항목이 일치**한다(비교 가능한 기존 프리셋 전부). 예외는 크림슨우드 성채 보스 5종뿐인데, 그쪽은 이 덤프가 **더 나중 상태**를 담고 있다 — 아래 "크림슨우드 성채 보스" 절 참고 |
| [akhuting/gms083](https://github.com/akhuting/gms083) | 스킬 WZ 데이터, 영문 몹 이름, ms079에 없는 몹 노드 | GMS v83의 `wz/Skill.wz/*.img.xml`(베놈 `mad`/`prop`/`time`의 출처), `wz/String.wz/Mob.img.xml`(영문명, 몹 ID 검증용), `wz/Mob.wz/*.img.xml`. **이 덤프는 API v83과 9종이 어긋난다**(루루모 HP 760만, 셰이드 · 선인인형 · 리치 · 대나무 무사 · 자동경비시스템 · 디트와 로이 등 회피 999 계열) **+ 9400589는 아예 다른 몹이다.** ms079에 파일이 없을 때만 쓰고, `info` 수치가 API와 맞는지 확인하고 쓴다 |
| 두 저장소의 `wz/Skill.wz/MobSkill.img.xml` | 몹 스킬 수치 | 공격업(110 / 111) 배율과 프리셋 `mobSkills`의 `x` 출처. v079와 v083 값이 같다 |
| 두 저장소의 `wz/Base.wz/StandardPDD.img.xml` | 표준 물리 방어력 표 | 피격 물리 데미지가 읽는 직업군 x 레벨 표. v079와 v083 값이 완전히 같은 것을 확인했다 |
| [Sajandora/Mapleland-Discord-InfoBot](https://github.com/Sajandora/Mapleland-Discord-InfoBot) | 한글 몹 이름 ↔ 몹 ID | `data/mobs.json`. WZ는 영문명이라 프리셋(한글명)과 몹 ID를 잇는 데 필요하다. 메이플랜드에 실제로 있는 몹 목록 역할도 한다 |
| [maplestory.io 다른 버전 · 리전](https://maplestory.io/api/kms/316/mob/) | 한글 몹 이름, 출현 맵 보강 | `kms/316`은 몹 이름이 한글이라 혼테일 파트처럼 InfoBot에 없는 몹의 이름을 여기서 가져온다. `gms/176`은 v83에서 비어 있는 `foundAt`을 채워 준다(뉴리프시티 · 크림슨우드 구분에 썼다) |

몹 속성 참고:
- `elemAttr`는 `"I2F3"` 같은 문자열이고, 속성 인덱스 4(문자 `S`)가 독이다.
  문자는 P 물리 / I 냉기 / F 불 / L 전기 / S 독 / H 성 / D 암.
- 값은 1 = 무효, 2 = 반감, 3 = 약점. 베놈은 1과 2에서 걸리지 않는다(약점이어도 증폭은 없다).
- `monsterPresets.ts`의 `id`가 곧 Mob.wz 몹 ID라 나중에 속성 데이터를 다시 붙일 수 있다.

## 몬스터 프리셋 데이터

`id`는 원작 Mob.wz 몹 ID다. 한글 이름으로 몹 ID를 찾고 레벨/HP로 교차검증해서 붙였다.
438종이 들어 있다.

### 기준 버전은 GMS v83이고, 수치는 maplestory.io API에서 본다

몹 수치의 1차 출처는 **maplestory.io의 `gms/83` 몹 API**다
(`https://maplestory.io/api/gms/83/mob/{id}`). `MonsterSprite`가 그림을 받아 오는 버전과
같아서 프리셋과 그림이 어긋나지 않는다.

전에는 `mrzhqiang/ms079`의 v0.79 XML을 먼저 봤는데, **기존 368종은 v79와 v83이 완전히
같다** — 레벨 · HP · 방어력 · 회피 · 명중 · 공격력 · 넉백 · 언데드 · 속성 · 보스 여부를
전수 대조해서 확인했다. 기준을 83으로 올려도 기존 값은 하나도 안 바뀐다. 반대로 GitHub의
XML 덤프는 파일마다 버전이 섞여 있어서(9400589 하나에 대해 ms079 · gms083 · API가
전부 다른 값을 준다) 수치는 API를 정본으로 본다.

**더 올리면 안 된다.**

| 올릴 버전 | 기존 프리셋에서 바뀌는 것 |
| --- | --- |
| v83 | 없다. 전 항목이 v79와 같다 |
| v87 / v92 | 속성 저항만 61종 바뀐다. 언데드 계열에 암(`D`) 저항이 붙고 시간의 신전 8종이 재조정된다. **독(`S`)이 무효 · 반감으로 바뀌는 몹은 없어서 베놈 판정은 그대로다** |
| **v93 이상** | 빅뱅 리밸런스다. 명중 368종 · 회피 362종 · 경험치 347종 · HP 333종 · 레벨 314종이 바뀐다(달팽이 HP 8 -> 15, 듀얼 비틀 레벨 76 -> 105). 메이플랜드는 빅뱅 전이라 쓸 수 없다. **v92가 빅뱅 직전 마지막 버전이다** |

API가 안 주는 것은 여전히 WZ XML에서 뽑는다 — `attack{n}` 노드(`physicalAttackPowers`,
`hasMagicAttack`), `info/skill`(`mobSkills`), 루트의 `fly` / `jump` / `move`(`cannotMove`).
`akhuting/gms083`의 `wz/Mob.wz/*.img.xml`을 쓰되 **그 파일의 `info` 수치가 API와 맞는지
몹마다 확인하고 쓴다.** 안 맞으면 그 몹은 넣지 않는다.

### 넣지 않은 몹

- **v83에서 수치가 완전히 같은 중복 몹**: 9400541(킬라 비) · 9400550(부머) ·
  9400585(크림슨 트리) · 9400587(팬텀 트리). 이동 속도 필드만 다르거나 아예 같다.
  9400550은 부머가 소환하는 대상이라 `summons`에는 남아 있고, 프리셋에 없으니 눌러도
  안 넘어간다.
- **혼테일(8810018)**: HP 20.9억짜리 항목인데 공격력 · 방어력 · 명중 · 회피가 전부 0이다.
  때리는 대상이 아니라 전체 HP 게이지용 껍데기다.
- **죽은 혼테일 파트(8810010~8810017)**: 파트를 잡은 뒤의 시체 상태다.
- **성채 보스가 소환하는 몹 3종(9400596 · 9400597 · 9400599)**: 한글 이름을 찾을 수
  없어서 넣지 않았다. `summons` 줄에는 v83 영문명으로 남아 있고 눌리지 않는다.
- **카오스 패츠(8800116)**: 카오스 자쿰과 같은 ID 대역에 있지만 어떤 몹의 소환 스킬에도
  안 나오고 `foundAt`도 비어 있어서, 그 전투에 실제로 나오는지 확인하지 못했다.

### 카오스 자쿰 16종만 기준 버전이 아니라 GMS v88이다

카오스 자쿰은 **기준 버전 v83에 아예 없다**(API가 404다). 처음 들어온 것이 v88이라
카오스 자쿰 본체 3종 · 팔 8종 · 소환 대상 5종(카오스 부기 · 푸코 · 푼코 · 쿠스코 ·
오파츠)만 `gms/88` 값을 쓴다.

**v88은 여전히 빅뱅 전이라 기준을 벗어나는 위험이 없다.** 확인한 것들이다.

- **v88 · v92 값이 완전히 같다.** 16종 전 항목을 대조했다. 빅뱅 전 구간(v88~v92)
  안에서는 어느 버전을 골라도 같은 값이므로 "v88이라서 달라진 것"이 없다.
- **v93부터는 빅뱅 값이다.** 명중 500 -> 169, 회피 24~32 -> 144처럼 전 몹 공통 공식으로
  덮어써지고 HP도 5.28억 -> 7.48억으로 바뀐다. 크림슨우드 성채 보스와 같은 이유로 쓸 수 없다.
- 수치가 원작 자쿰의 배수로 딱 떨어진다 — HP는 8배(6600만 -> 5억 2800만), 물리 공격력은
  1.2배(980 -> 1176), 방어력은 그대로다. 별개로 만든 데이터가 아니라 자쿰을 스케일한
  것이라는 뜻이다.

API가 안 주는 노드(`attack{n}` · `info/skill` · 루트의 `fly` / `jump` / `move`)는
`xDazedGamingx/LostStoryV90`의 v90 `wz/Mob.wz/*.img.xml`에서 뽑았다. ms079(v0.79)와
gms083에는 이 몹들의 파일이 아예 없다. **16종 전부 그 XML의 `info` 수치가 API v88과
일치하는 것을 확인하고 썼다** — 다른 덤프를 쓸 때와 같은 절차다.

**메이플랜드 실측이 아직 없다.** 레벨 · HP · 방어력 · 회피 · 경험치는 원래 실측을
우선하는 항목인데, 카오스 자쿰은 대조할 실측값을 찾지 못해 전부 WZ 값을 그대로 넣었다.
확인되는 대로 프리셋을 고치면 된다.

### 크림슨우드 성채 보스는 기준 버전이 아니라 ms079를 쓴다

성채 파티퀘 보스 5종(나그롬 · 마르가나 · 레드너그 · 렐릭 · 흐살프)만 **기준 버전 v83이
아니라 ms079 덤프 값**을 쓴다. 이 몹들은 v83 이후에 리밸런싱됐고, GMS 라인에서는 그
리밸런싱이 **빅뱅(v93)과 같은 패치에 묶여 들어와서** v83과 v93 사이에 "리밸런싱은 됐지만
빅뱅은 안 된" 버전이 없다.

| 출처 | 레벨 | HP (나그롬 / 마르가나 / 레드너그 / 렐릭 / 흐살프) | 명중 · 회피 · 공격력 |
| --- | --- | --- | --- |
| API v79~v92 | 130 | 9400589는 **다른 몹**(레벨 30 / HP 40만) · 2.0 / 2.55 / 2.31 / 2.33억 | 빅뱅 전 |
| API v93 이상 | 120 | 2.4 / 2.0 / 2.1 / 2.2 / 2.3억 | **빅뱅 후**(명중 144 · 회피 121로 전부 덮어써짐) |
| **ms079** | **120** | **2.4 / 2.0 / 2.1 / 2.2 / 2.3억** | **빅뱅 전**(명중 230~300 · 회피 39~55) |

ms079를 고른 근거는 이렇다.

- **HP · 방어력 · 넉백 · 속성 · HP 회복이 v93과 정확히 같다.** 두 덤프가 독립적으로 같은
  리밸런싱 결과를 가리킨다. HP는 2.0 / 2.1 / 2.2 / 2.3 / 2.4억으로 딱 떨어지는 계단이라
  우연이 아니다.
- **명중 · 회피 · 공격력 · 경험치는 빅뱅 전 값이다.** v93은 이 넷을 전 몹 공통 공식으로
  덮어써서(명중 144 · 회피 121) 그대로 가져오면 회피 확률과 피격 데미지가 통째로 틀어진다.
- ms079는 나머지 359종에서 API v83과 **한 항목도 다르지 않다.** 이 5종만 다른 것은
  덤프가 섞여서가 아니라 콘텐츠가 실제로 갱신됐기 때문이다.
- gms083은 9400589 자리에 아예 다른 몹(레벨 180 / HP 1700만)을 담고 있고,
  흐살프 물리 방어력을 8000으로 적는다 — 원작 방어력 상한(1999)을 넘는 값이라 못 쓴다.
  ms079와 v93은 둘 다 1999다.
- 스킬 목록도 ms079 쪽이 갱신본이다. v83 `MobSkill.img`의 소환 174 · 175 레벨은 아직
  `info = "일본"` 자리표시자인데 ms079에는 소환 대상이 채워져 있다.

**아직 실측으로 확인한 것은 아니다.** 확인되면 이 절과 값을 같이 고치면 된다.

**레벨 / HP / 방어력 / 회피 / 경험치는 원작 WZ로 덮어쓰지 않는다.**
이 값들은 메이플랜드 실측 기준이고, 원작과 다른 몹이 17종 있다.
루루모처럼 원작 데이터 쪽이 이상한 경우도 있어서 프리셋 값을 우선한다.
원작과 다른 항목은 `git log`에서 프리셋 커밋 메시지에 정리해 뒀다.

반대로 아래 항목은 계산에 쓰이지 않거나 실측이 어려워 기준 버전 WZ 값을 그대로 쓴다.
`accuracy`, `physicalAttack`, `magicAttack`, `minimumPushDamage`,
`isUndead`, `elementAttributes`, `poisonAttribute`, `isBoss`.

`accuracy`는 회피 확률이, `physicalAttack` / `magicAttack`은 피격 데미지가,
`minimumPushDamage`는 넉백 확률이 붙으면서 실제로 계산에 들어간다. 그래도 WZ 값을 그대로 두는 이유는, 메이플랜드가 이 값을 손댔다는
증거가 레벨·HP·방어력과 달리 아직 없기 때문이다. `accuracy`는 실측 수단 자체가 없지만
(회피는 성공/실패만 보이고 수치가 안 뜬다), **공격력은 피격 데미지에서 역산할 수 있으므로
확인되는 대로 프리셋을 고치면 된다.**

**프리셋에 없는 값은 "모른다"가 아니라 "없다"로 읽는다.** 프리셋은 WZ에서 전수로
뽑았으므로 빠진 항목은 그 몹에 그 속성이 없다는 뜻이다(`hasMagicAttack`,
`physicalAttackPowers`, `mobSkills`, `cannotMove` 전부 같다).
값을 모르는 것은 직접 입력 몬스터뿐이다 — 그래서 직접 입력에서는 `minimumPushDamage`를
비워 두고 넉백 확률을 아예 그리지 않는다.

`mobSkills`는 `Mob.wz`의 `info/skill`에서 뽑고, `x`는 그 몹이 쓰는 레벨의
`MobSkill.img` 값이다. 계산에는 안 들어가고 화면 경고에만 쓴다.

`physicalAttackPowers`(몸박보다 센 물리 공격들의 공격력)와 `hasMagicAttack`(마법 공격
보유 여부)은 `Mob.wz`의 `attack{n}/info`에서 뽑았다. 원작이 `max(몹 기본, 공격별)`을
쓰므로 몸박 이하인 값은 넣지 않고, 같은 값은 한 번만 넣는다. 둘 다 피격 데미지에만 쓰인다.

`cannotMove`(이동 능력 없음)는 `Mob.wz` 몹 루트에 `fly` / `jump` / `move` 노드가
하나도 없는 몹에 붙였다(63종, v079 · v083 일치). 넉백 확률에만 쓰인다 — 자세한 내용은
위 "넉백 확률" 절에 있다.

`region`은 몹의 출현 맵(maplestory.io `foundAt`)을 기존 프리셋의 지역 라벨과 대조해
정했다. 출현 맵 정보가 없는 몹(주로 소환형 보스)은 추정하지 않고 `'기타'`로 뒀다.
잘못된 지역보다 `'기타'`가 낫다고 보고 내린 선택이라, 확인되는 대로 채워 넣으면 된다.
혼테일 파트 10종과 혼테일 동굴의 와이번 · 코니언이 `'기타'`인 것도 같은 이유다
(이미 있던 다크 와이번 8810021과 같은 자리에 놓인다).

**카오스 자쿰 16종은 자쿰과 똑같이 갈랐다.** 어느 버전에서도 `foundAt`이 비어 있어
지역을 정할 근거가 없는데, 자쿰이 이미 **3페이즈만 `'엘나스'`, 나머지(1 · 2페이즈와 팔
8종)는 `'기타'`** 로 들어가 있다. 같은 제단에서 같은 구성으로 싸우는 몹을 다르게 놓으면
드롭다운에서 자쿰과 카오스 자쿰이 서로 다른 자리에 흩어진다. 소환 대상 5종은 자쿰이
부르는 부기 · 푸코 계열과 달리 필드에 나오지 않으므로 `'기타'`다.

**`'뉴리프시티'` / `'크림슨우드'`는 v176의 `foundAt`으로 갈랐다.** 이 지역 몹들은 v83에서
`foundAt`이 비어 있어서 기준 버전만으로는 지역을 못 정한다. v176에서는 `6000xxxxx`(뉴리프
시티)와 `6100xxxxx`(크림슨우드)로 깨끗하게 갈리고, 이걸로 **머리없는 기수가 뉴리프시티가
아니라 크림슨우드(`610010010`)에 있다**는 것도 확인했다. 가져온 것은 지역뿐이고 수치는
전부 v83이다.

**`'미출시'`는 지역이 아니라 출시 상태 라벨이다.** 핑크빈과 그 동상 5종 · 미니빈은
메이플랜드에 아직 없어서 시간의 신전이 아니라 여기로 뒀다.

## 개발 규칙

### localStorage 키 (오리진 공유)

이 앱은 **다른 유틸들과 브라우저 저장소를 공유한다.**

- `https://myungwoo.github.io/damage-calculator/` — 다른 프로젝트 페이지들과 오리진
  (`myungwoo.github.io`)이 같다.
- `https://mapleland.myungwoo.kr/damage/` —
  [메이플랜드 유틸 모음](https://github.com/myungwoo/mapleland-utils)이 유틸 다섯 개를
  한 도메인의 하위 경로로 함께 배포한다.

**localStorage 는 오리진 단위다. 경로로 갈라지지 않는다.** `/damage` 와 `/hunt` 는 같은
저장소를 본다. 그래서 접두어 없는 키는 다른 유틸의 값을 조용히 덮어쓴다.

- **앱 전용 값은 `ml:damage:` 로 시작한다.** 저장 슬롯은 `ml:damage:save:N`
  (`app/constants/calculator.ts`).
- **테마는 일부러 공유한다.** 키는 `ml:theme`, 값은 `'light' | 'dark' | 'system'`
  (`app/constants/theme.ts`). 값 집합을 앱마다 다르게 해석하면 안 된다 —
  예전에 이 앱이 `'system'` 을 무효 값으로 보고 덮어써서, 사냥 타이머에서 고른
  "시스템 설정"을 지우는 버그가 있었다. **모르는 값은 시스템 설정으로 보고 덮어쓰지
  않는다**가 규칙이다. `app/layout.tsx` 의 프리-페인트 스크립트와 `ThemeToggle` 이
  같은 규칙을 지켜야 한다.
- **키 이름을 바꿀 때 예전 키를 지우지 않는다.** 새 키가 비어 있을 때만 한 번 복사한다
  (`app/utils/saveStorage.ts`). 배포를 되돌려도 저장이 남아 있어야 하고, 그래야
  마이그레이션이 여러 번 돌아도 결과가 같다. `tests/saveStorage.test.ts` 가 이걸 지킨다.

### 코드 스타일
포맷 규칙의 단일 출처는 `.prettierrc.json`이다. ESLint의 `prettier/prettier` 규칙도
옵션을 따로 적지 않고 이 파일을 읽으므로, 규칙을 바꿀 땐 여기만 고친다.
- 들여쓰기: 스페이스 2칸
- 세미콜론: 사용
- 따옴표: 작은따옴표
- 주석: 한국어로 작성

`npm run format:check`가 CI에서 도는 검사와 같다. 손으로 표와 줄바꿈을 맞춘
문서(`*.md`)와 빌드 산출물은 `.prettierignore`로 제외했다.

### CI
`.github/workflows/ci.yml`이 검사의 단일 출처다.
포맷 -> 린트 -> 타입 -> 테스트 순으로 돌고, PR에서는 빌드까지 확인한다.
배포 워크플로우(`nextjs.yml`)는 이 파일을 `workflow_call`로 재사용해
`checks` 잡이 통과해야만 빌드·배포가 진행된다. 검사를 추가할 땐 `ci.yml`만 고치면
PR과 배포 양쪽에 같이 걸린다.

### 문서 유지보수
- 코드 수정 시 CLAUDE.md도 함께 업데이트하여 최신 상태 유지
- 새로운 파일/모듈 추가 시 프로젝트 구조에 반영
- 새로운 명령어 추가 시 주요 명령어에 반영
- 변경 사항이 README.md에 영향이 있는 경우 함께 수정

### README.md 기조
- 인간 개발자를 위한 실용적인 내용
- 맨 위에 배지 배치
- 적절한 이모지 사용
- 기술적 세부사항은 CLAUDE.md에, 운영 가이드는 README.md에

### 커밋 메시지 형식
```
Type: 설명

(필요시 상세 설명)

Co-Authored By는 포함하지 마세요.
```

**Type 종류**: Fix, Feat, Refactor, Docs, Chore

### 예시
```
Fix: 크리티컬 데미지 계산 오류 수정
Feat: 몬스터 프리셋 선택 기능 추가
Refactor: 데미지 계산 로직 분리
```

## 주요 명령어

```bash
npm run dev      # 개발 서버 실행 (포트 3000)
npm run build    # 프로덕션 빌드
npm run lint     # ESLint 검사
npm run lint:fix # ESLint 자동 수정
npm run format   # Prettier 자동 포맷
npm run format:check # Prettier 포맷 검사 (CI와 동일)
npm run typecheck    # tsc --noEmit 타입 검사
npm test         # 유닛 테스트 (tsconfig.test.json으로 컴파일 후 node --test)
```

테스트는 별도 러너 의존성 없이 `tsc -p tsconfig.test.json`으로 `.test-build`에 컴파일한 뒤
Node 내장 테스트 러너로 실행한다. 방컷 확률은 배포 코드와 독립적으로 작성한
참조 DP, 몬테카를로 시뮬레이션과 대조해 검증한다.
베놈도 마찬가지로 게임 진행을 그대로 흉내 낸 몬테카를로(`simulateKillProbabilitiesWithVenom`)와
대조한다. 확률 계산 로직을 손대면 이 대조를 반드시 다시 돌린다.
